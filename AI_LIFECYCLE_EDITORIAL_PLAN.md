# 生命周期文档编辑完善计划

> 版本：1.0 · 状态：已完成
>
> 用途：文档编辑任务及验收依据。实际结果见[执行状态](execution/status.md)；方法论建设任务另行安排。

## 1. 目标与范围

让首次接触仓库的读者直接理解模型是什么、如何使用、每项活动产生什么制品，无需了解此前聊天、仓库清理和方案调整过程。

本任务调整正文表达、阅读入口、维护记录和工具提示。模型本身的功能范围、活动、制品、提示词及验收要求保持不变，不执行完整方法论建设，不生成应用、四项详细活动指南、A23–A27 或图解，不提交或推送。

执行前读取根 [AGENTS.md](AGENTS.md)、本计划、[执行状态](execution/status.md)、[方法论建设计划](AI_LIFECYCLE_V03_REFINEMENT_PLAN.md)、[模型总纲](model/overview.md)及[约定](model/conventions.md)。涉及教学目录时读取对应 AGENTS。

删除已完成清理材料是本任务的明确范围。与现有文件中“保留清理记录”“先读清理计划”等旧要求冲突时，以本计划及用户本次指令为准。其他工作区保护、敏感信息和证据真实性规则继续适用。

成功标准：正文能够独立阅读，当前任务可接手，未完成状态准确，历史材料及其运行依赖已移除，文档和工具检查通过。

## 2. 内容调整规则

### 2.1 采用直接定义

文档面向方法的使用者，使用“目标、规则、输入、动作、输出、适用条件、判断标准”组织内容。删除只有参与过讨论才理解的前后对照、决定经过和修订说明。

| 表达类别 | 调整示例 |
| --- | --- |
| 讨论结论 | “最新方向以演化型原型为核心”改为“本模型以演化型原型为核心” |
| 决策来历 | “已确认的方法论与默认规则”改为“模型原则与工作规则” |
| 仓库经过 | “清理后的唯一模型修订方案”改为计划的目标与交付说明 |
| 辩论式解释 | “不是抛弃型原型”改为直接定义原型演化的输入、行为与版本关系 |
| 工具历史 | “新版图解待建立”改为描述缺失输入的诊断 |
| 重复进度 | 各处“仍待下一轮补强”收敛到状态入口及相关建设任务 |

这些是语义判断规则，不做全仓机械替换。“需求修订、体验反馈、版本演化、已有授权、确认来源、前一候选及证据失效”属于方法论机制，应保留。技术迁移、版本号、ADR、功能变更教学案例也不因含有历史词语而删除。

优先用正面要求说明行为，只有会影响理解或执行的边界才保留限制性表述。保留安全、权限和证据约束，避免把简洁理解为删除工程责任。

### 2.2 不改变的方法论

- 四项活动名称及职责：原型构建、原型重构、原型审查、原型精修。
- 构建包含需求获取、分析、设计、实现、体验反馈和澄清；原型可在替换全部实现代码的情况下持续演化。
- 重构对齐工程约束，同步实现、测试和稳定工作规则；审查提供覆盖记录及面向人的实现解读。
- 发现一个问题后，检查全项目相同模式和语义变体，分类、修复、回归并再次排查。
- 精修支持设计 AI、人工和混合来源；行为变化按影响更新规格和验证。
- 活动可按功能、迭代或混合方式裁剪组合，适用质量责任保留。
- 人驱动 AI 的操作提示词、独立验收、直接本地发布维护及教学证据边界。

### 2.3 按读者分配内容

| 材料 | 应包含 | 应移除或归并 |
| --- | --- | --- |
| README | 定位、使用路径、制品和示例入口、检查与贡献 | 讨论经过、清理完成说明、删除数量、快照、历次授权 |
| 模型正文 | 规则、流程、制品内容、适用条件及可操作提示词 | “新版改成”“此前方案”“本次保留”等仓库演进叙述 |
| 根/教学 AGENTS | 稳定资料入口、工作边界、验证与交付规则 | 清理专用指令、临时任务进度、提交次数限制的历史背景 |
| 建设计划 | 目标、实际起点、依赖、交付、验收及执行文本 | 对已完成清理、旧快照或不存在工作区差异的依赖 |
| 执行状态 | 当前已有内容、当前任务、待完成任务及下一步 | CLEAN 任务表、旧提交推送经过、长期保留的过程流水账 |
| 教学示例 | 共同输入、角色、业务规则、输出及检查方法 | 从真实对话来历推导教学授权的表述、无用途的重复免责说明 |

README 的主要阅读顺序为：模型总纲 → 制品目录 → 实践与提示词 → 教学上下文和填写示例。实施计划与状态放在贡献/维护入口，不要求使用者先阅读仓库建设过程。

## 3. 文件、状态与工具调整

### 3.1 删除三份已完成材料

只删除以下三个文件，不新建档案或迁移空壳：

```text
AI_LIFECYCLE_V03_CLEANUP_PLAN.md
execution/cleanup.md
execution/cleanup-files.json
```

执行前确认目标仍是已入库文件，核对 Git 差异，避免覆盖新改动。直接删除并同步修复引用，不需要额外提交或复制备份；历史内容已可通过 Git 查阅。除这三项外不删除其他文件，不改变 Git 历史。

面向读者的文档、AGENTS、建设计划、状态和工具不得再要求读取这些文件。本文中的删除清单属于执行目标说明，以代码文本保留，不作为内容依赖；不要创建指向将被删除文件的 Markdown 链接。

### 3.2 建设计划与状态

保留 `AI_LIFECYCLE_V03_REFINEMENT_PLAN.md` 路径，标题改为“人驱动 AI 软件生命周期模型：方法论建设计划”。保留版本元数据，计划版本递增；版本标记是文档标识，不是讨论痕迹。

按以下要求完善计划，但不执行其中的任务：

- 将 V03-01 改为“现状盘点与任务准备”，读取实际仓库、确认输入和文件边界、建立状态，不依赖清理记录或清理前提交。
- 将 AC-V03-09 改为“范围与引用一致”，核对文件范围、有效引用和教学独立性，不以保留清理历史为标准。
- 删除“保留当前未提交清理结果”等过时前提，改为执行时检查并保护实际存在的用户改动。
- goal 启动文本仅依赖当前 AGENTS、状态、计划及模型文件。
- 完整保留四项活动、A01–A27 目标、九类制品提示词、七类活动/阶段提示词、7 项任务、10 项验收标准及 12 个走查场景，不只保留编号而删掉要求。

`execution/status.md` 是进度事实的集中入口。列出实际存在的 A01–A22、通用实践、三份后续阶段指南及教学材料；四项详细活动指南、A23–A27、图解和实证仍为未完成或未开展。V03-01 至 V03-07 不因编辑任务完成而更改完成状态。

README 可保留一句“文档覆盖与建设进度见执行状态”。制品目录只列现有可访问材料，未产生制品的目标保存在建设计划中。图解说明可直接注明源码未提供及所需输入，不绘制不存在的成果，也不把当前基础描述为完整模型。

### 3.3 教学示例

逐篇核对 FocusTask 上下文及 A01–A22。保持创建任务 AC-01–AC-13、权限、字段、幂等、筛选、异常及质量设定不变，实例 ID 和文件路径不变。

每份教学例保留“教学填写示例”“不作通过结论”，采用一条简明来源说明，例如“来源：FocusTask 教学上下文；本例为教学设定”。检查项未执行时继续如实标注，不能因去除重复说明而制造确认或成功结果。

规范和模板中的目标项目路径、输入占位符、文档版本以及正常的用户确认流程可以保留。模板章节、提示词标识和索引关系不改变。仅对确有叙述问题的文件改写；无问题的文件记录已检查即可。

### 3.4 工具行为与接口

渲染器缺输入时的诊断统一为：

```text
缺少图解源码：{缺失的 .mmd 文件列表}
```

保持退出码为 1、列出具体缺失文件、在调用外部渲染工具前失败。同步更新说明、测试名称、断言和虚拟工具路径，例如将测试路径改为 `/nonexistent/test-mmdc`。

检查器只修改确有仓库演进痕迹的注释或测试描述，当前 CLI、JSON 结果字段、22 类制品和三份现有指南检查保持不变。保留现有测试覆盖，不新增格式检查平台、不安装工具、不生成图解。

## 4. 执行顺序与恢复

| 任务 | 依赖 | 工作 | 完成条件 |
| --- | --- | --- | --- |
| EDIT-01 盘点与任务准备 | 无 | 核对 Git、规则、已有材料和目标引用，建立覆盖清单 | 明确三项删除和逐篇审阅范围，区分话题历史与业务机制 |
| EDIT-02 正文与入口 | EDIT-01 | 改写 README、模型、实践、制品、模板、教学例及 AGENTS | 定义直接、阅读独立、规则和教学语义保持 |
| EDIT-03 计划与记录 | EDIT-02 | 更新建设计划和当前状态，删除三份完成材料及全部依赖 | 没有断链或历史任务前置条件，未完成事项仍准确 |
| EDIT-04 工具措辞 | EDIT-03 | 更新渲染器诊断、说明及受影响测试/注释 | 行为及接口不变，语言中性，测试继续有效 |
| EDIT-05 验收与交付 | EDIT-04 | 执行第 5 节检查，修复后复查，更新状态 | 所有必需项通过，交付范围和下一步明确 |

每次推进一个可核对任务。在 `execution/status.md` 简要记录任务进度、覆盖情况、实际检查、阻塞及恢复位置，不另建讨论历史、清理报告或全文件快照。阅读覆盖清单可按目录列文件及结果，保留到最终验收。

执行时先检查实际工作区。遇到语义变化无法判断时只暂停相关内容，常规措辞和排版按本计划处理。恢复会话先读状态、计划和实际差异，继续未完成部分，不重做已完成任务或重复删除。

完成后只保留简短的本任务结果、真实检查及下一任务；不将过程流水账放入 README 或模型正文。本计划在任务完成后标记完成并保留为执行依据，不从面向读者的主阅读路径引用。

## 5. 验收标准与实际检查

| 编号 | 标准 | 可核对的证据 |
| --- | --- | --- |
| AC-EDIT-01 | 阅读独立 | README 直接介绍模型，读者从总纲找到制品和教学例，无需了解仓库历次讨论 |
| AC-EDIT-02 | 规则保持 | 第 2.2 节原则、活动职责、提示词操作链、验收和本地发布边界逐项保持 |
| AC-EDIT-03 | 历史材料退出 | 三份目标文件不存在；运行入口、正文及工具没有对它们的引用或前置依赖 |
| AC-EDIT-04 | 计划完整 | 建设计划保留 7 任务、10 标准、12 场景及相应内容，V03-01 和 AC-V03-09 已按当前仓库定义 |
| AC-EDIT-05 | 状态真实 | 现有与待完成内容清楚区分，编辑完成未被写成模型、图解或实证完成 |
| AC-EDIT-06 | 教学有效 | 22 例保留边界标记，业务规则与上下文一致，模板和实例可独立使用 |
| AC-EDIT-07 | 工具与格式 | 文档检查、现有测试及差异检查通过；渲染器缺源码按新文本预期失败 |
| AC-EDIT-08 | 范围可控 | 仅三项文件删除和范围内编辑，无应用、新制品、图稿或外部写入；未提交推送 |

在仓库根运行并记录退出码及实际结果：

```sh
node scripts/check-docs.mjs
node --test scripts/check-docs.test.mjs
node scripts/render-model-diagrams.mjs
git diff --check
git status --short
git diff --stat
```

渲染命令预期退出 1，检查“缺少图解源码”及具体文件名；这是诊断行为验证，不是渲染通过。文档、测试、差异检查应退出 0，测试不减少、不跳过来规避失败。

语义验收必须逐篇审阅保留的 Markdown：README、两份 AGENTS、建设计划、所有模型/实践/阶段/制品规范、模板和教学文档。关键词搜索只用来发现候选，不用“零命中”替代阅读。任务计划及状态中必要的范围、验收和版本描述可以保留，记录理由；本文为执行任务而列出的改写示例和删除目标不属于读者正文残留。

检查“旧版、新版、此前、本次讨论、清理前后、用户随后授权”等表达是否还承担仓库叙事；检查“反馈、版本、迁移、修订、确认”等正常领域概念没有被误删。特别核对精修的局部功能变化、整体代码替换、逐行审查和全项目举一反三的规则。

最终交付实际改写与删除范围、代表性改写、检查结果和剩余建设任务。必需项通过后只报告“文档编辑完善完成”；下一项仍为方法论建设计划的 V03-01，不自动启动。

## 6. 交给 goal 的执行文本

```text
请执行 AI_LIFECYCLE_EDITORIAL_PLAN.md，按 EDIT-01 至 EDIT-05 完成文档编辑完善。
先读取根 AGENTS、当前状态、本计划、方法论建设计划、模型总纲和约定；
涉及 FocusTask 时读取其目录规则。核对当前工作区，保护无关改动。

将正文写成首次读者能够独立理解的方法论，删除历次讨论、仓库清理和
方案演进叙述；保留原型演化、需求反馈、确认来源、版本与证据等业务机制。
整理阅读入口，把建设进度集中到执行状态，保持尚未完成内容的真实状态。

直接删除本计划指定的三份已完成清理材料及其依赖，不建档案或兼容页。
更新建设计划的起点、V03-01、AC-V03-09 和 goal 文本；
保留四项活动、A01–A27 目标、提示词要求、7 任务、10 标准和12 场景。
保持教学业务规则、22 份示例、模板结构和教学边界，不生成新增模型制品。

将渲染器提示改为“缺少图解源码”，同步文档和测试，保持退出行为和接口。
在 execution/status.md 记录简洁进度与恢复位置，逐篇完成语义审阅，
执行计划规定的文档、测试、诊断和差异检查，修复后重验。

只在 AC-EDIT-01 至 AC-EDIT-08 均满足后报告文档编辑完善完成。
不执行完整方法论建设，不启动应用或实证，不提交或推送。
交付改写/删除范围、检查结果和下一任务，不能把计划内容冒充已实现成果。
```
