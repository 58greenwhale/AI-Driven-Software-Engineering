# A29 系统蓝图建设计划

> 计划版本：1.0 · 目标模型版本：0.5 · 状态：已完成

## 12. 实际执行记录

- A29 规范、模板、FocusTask 教学例、索引和目录已建立。
- 检查器已扩展到 A01–A29；A28/A29 使用 6 类提示词，A01–A27 保持原有 9 类提示词。
- A29 已接入 A28、A03、原型构建入口、README、总纲、约定和执行状态。
- Mermaid 源码已更新并重新导出 4 组 SVG/PNG；`artifacts.png` 已完成视觉核对，A29 系统蓝图和 A29→A28 关系可读。
- `node scripts/check-docs.mjs`：退出 0，`files: 119`、`localLinks: 386`、`artifacts: 29`、`activities: 4`、`stages: 3`、`errors: []`。
- `node --test scripts/check-docs.test.mjs`：44 项通过，0 失败/跳过。
- `git diff --check`：退出 0。
>
> 用途：为每个项目建立一份系统级上下文，连接项目目标、系统模块、跨模块流程、外部系统和 A28 模块蓝图，作为 AI 进入模块和功能实现前的全局 handoff 入口。

## 1. 目标与背景

A28 已为单个业务模块提供模块边界、能力地图、功能点和模块级 handoff，但多个模块之间仍缺少统一的系统级入口。没有系统蓝图时，AI 需要从 A01、A04 和多个 A28 中自行重建系统模块地图、跨模块旅程、外部依赖和全局风险，容易重复定义边界，遗漏跨模块约束，或只按单模块局部正确地推进原型。

本计划新增 A29“系统蓝图”。一个项目对应一份 A29。A29 由主模型根据现有资料整理草稿，开发人员回答会改变系统边界或全局规则的问题并确认版本；条件允许时可由其他模型独立只读复核，再由开发人员决定是否修订。A29 最终 Markdown 作为系统级 handoff 入口，指导 AI 先理解系统，再读取相关 A28、A02、A03、A04 和 A10。

整体关系：

```text
项目目标与范围（A01）
  -> 系统蓝图（A29）
  -> 模块蓝图（多个 A28）
  -> 领域与权限规则（A02）/架构与契约（A04）
  -> 功能点规格（多个 A03）
  -> 设计与实施计划（A10）
  -> 原型构建与后续活动
```

## 2. 范围与边界

### 2.1 范围内

- 新增 A29 规范、空白模板和 FocusTask 教学填写示例。
- 将 A29 纳入 `model/artifact-index.json`、`model/artifact-catalog.md`、`model/conventions.md` 和文档检查器。
- 将现有制品范围从 A01–A28 扩展为 A01–A29，保持 A01–A28 的编号、路径和语义稳定。
- 更新 README、总纲、A01/A04/A10 入口、A28 输入说明和相关活动指南，使 AI 先读取 A29 系统上下文，再进入模块与功能。
- 更新 `model/diagrams/artifacts.mmd`，表达 A29 与 A01、A02、A04、A10、A28 的系统级关系，并重新生成 SVG/PNG。
- 增加 A29 的生成、澄清、复核、修订和交接提示词，以及检查器正例和反例测试。

### 2.2 范围外

- 不创建真实应用、数据库、接口服务、部署环境或运行证据。
- 不把 A29 变成项目管理系统、架构决策系统或自动工作流引擎。
- 不复制 A01 的完整项目目标、A02 的完整领域/权限规则、A04 的完整技术契约、A28 的模块细节或 A10 的实施排期。
- 不要求必须使用其他模型复核；复核建议不能自动成为开发人员确认。
- 不重写 A01–A28 的历史结论；只同步必要的范围、链接和下游入口。

## 3. 权威边界与去重规则

A29 是系统级地图、摘要、关系和索引，不是新的业务或技术真值。所有摘要必须记录来源和版本；发生冲突时标记冲突、影响和待决定事项，不覆盖权威制品。

| 内容 | 权威制品 | A29 中允许保留的内容 |
| --- | --- | --- |
| 项目愿景、版本范围、项目级成功条件 | A01 | 系统目标、边界摘要和 A01 版本引用 |
| 模块内部实体、状态、权限和一致性规则 | A02/A28 | 系统级不变量摘要、模块入口和来源 |
| 单项功能流程、异常、AC 和测试映射 | A03 | 跨模块旅程中的功能名称、顺序和 A03 入口 |
| 技术组件、接口字段、依赖方向和非功能约束 | A04 | 组件/外部系统关系摘要和 A04 入口 |
| 单模块能力、功能清单和模块边界 | A28 | 模块地图、模块职责、状态和 A28 版本 |
| 本轮任务、并行和集成条件 | A10 | 系统级实施顺序摘要和 A10 入口 |
| 验证、追踪和证据 | A07/A14/A16 | 系统级风险、覆盖入口和待重验索引 |

A29 可以写“订单模块通过支付接口依赖支付模块”，但不能重新定义支付接口字段、订单权限矩阵或订单功能验收步骤。A29 变化不自动修改 A02/A03/A04/A28；权威制品变化后，A29 必须更新摘要和版本引用。

## 4. A29 文档结构

文件基名为 `A29-system-blueprint`，三类文件分别位于：

- `model/artifacts/A29-system-blueprint.md`
- `model/templates/A29-system-blueprint.md`
- `examples/focustask/docs/artifact-examples/A29-system-blueprint.md`

规范和模板必须包含以下章节：

1. 用途与生命周期：系统级触发条件、生产者、责任角色、输入、使用者、输出和关联制品。
2. 系统定位与边界：系统目标、业务上下文、包含范围、非目标和 A01 关系。
3. 模块地图：模块 ID、名称、职责、状态、优先级、A28 路径和模块之间的依赖。
4. 外部系统与系统关系：外部参与者、系统、接口、事件、数据所有权和信任边界。
5. 跨模块旅程与关键流程：端到端流程、模块顺序、关键分支、失败传播和恢复边界。
6. 全局角色与不变量：跨模块角色边界、全局权限原则、数据一致性和系统级约束摘要。
7. 系统数据流与非功能摘要：主要数据流、性能/安全/可用性目标、监控和证据入口；详细约束引用 A04/A07。
8. 演化与实施顺序：模块依赖、当前实现状态、原型演化顺序、集成候选和 A10 入口。
9. 关联制品与版本：A01/A02/A04/A07/A10/A16 以及所有 A28 的路径、版本、状态和用途。
10. 未决事项与系统风险：问题、来源、影响范围、责任人、决定状态、变更传播和下一步。
11. 确认与 handoff：确认人、确认版本、复核意见、剩余风险、模块入口和下一条系统/模块指令。

每个项目只能有一个当前有效 A29 实例；版本变化保留旧版本关系，不覆盖历史确认。所有模块 A28 必须在 A29 中登记，A28 也必须记录所属 A29 路径和版本。

建议的实际项目布局：

```text
docs/
  system-blueprint.md
  modules/
    orders/module-blueprint.md
    payments/module-blueprint.md
```

其中 `system-blueprint.md` 是 A29，模块目录下的 `module-blueprint.md` 是 A28。项目可以沿用其他目录约定，但必须能从 A29 找到当前模块和下游功能入口。

## 5. 生成、确认、复核与交接

A29 与 A28 使用相同的六类专属操作，不复制 A01–A27 的完整九类操作链：

### A29-START：盘点系统资料

输入为 A01、A02、A04、A07、A10、已有 A28、外部系统资料、代码事实和项目约束。AI 列出已知事实、缺口、冲突、模块候选和需要开发人员回答的问题，不自行补全系统边界或全局业务规则。

### A29-GENERATE：整理系统蓝图

AI 按本规范和模板生成草稿，建立系统目标、模块地图、外部系统关系、跨模块旅程、全局约束摘要、演化顺序和关联制品索引。每项摘要记录来源；未知内容标为待确认、模拟或不适用。

### A29-CLARIFY：收集开发人员回答

当资料不足或冲突会改变系统边界、模块关系、数据所有权或全局规则时，AI 按影响排序提出问题和选项。开发人员回答后记录来源、适用范围、影响章节和需要同步的 A01/A02/A04/A28/A10。可从仓库发现的事实直接核对，不重复询问。

### A29-REVIEW：独立复核系统蓝图

开发人员可以自行复核，也可以交给其他模型只读复核。复核检查系统边界、模块覆盖、跨模块旅程、外部关系、来源一致性、重复定义、未决项和下游可实施性。复核者不能修改权威制品或把建议直接写成确认。

### A29-REVISE：按确认结果修订

开发人员决定哪些复核意见被采纳。AI 先列影响，再修订获准内容，更新 A28 清单、关联制品版本和失效/待重验项；没有确认来源的系统语义变化暂停相关修改。

### A29-HANDOFF：交付系统上下文

输出人工确认后的 Markdown、系统版本、模块/A28 清单、权威制品版本、跨模块风险、未决事项和下一条模块构建指令。A29 是系统级 handoff，不替代 A13 会话交接。

## 6. 索引、检查器与图解改动

- `model/artifact-index.json` 追加 A29，沿用 `id`、`title`、`spec`、`template`、`example` 字段。
- `model/artifact-catalog.md` 追加 A29 导航行，说明项目级触发、输出位置和下游用途。
- `model/conventions.md` 将制品范围扩展到 A01–A29，说明 A29 项目实例、版本和 A29/A28 关联规则。
- `scripts/check-docs.mjs` 将期望制品扩展到 29；A01–A27 保持 9 类提示词，A28/A29 各检查 `START`、`GENERATE`、`CLARIFY`、`REVIEW`、`REVISE`、`HANDOFF` 6 类提示词。
- `scripts/check-docs.test.mjs` 增加 A29 正例和反例：缺规范、缺模板、缺教学例、缺任一 A29 提示词、重复/越界编号、A29 链接断裂和教学边界缺失。
- 更新 README、总纲、A28 规范、A01/A04/A10 入口和相关活动指南，使 AI 先读取 A29 系统上下文，再读取模块和功能制品。
- 更新 `model/diagrams/artifacts.mmd` 表达 `A29 -> A28 -> A03`，以及 A29 到 A01/A02/A04/A10 的系统级摘要关系；只修改 Mermaid 源码后重新导出 SVG/PNG，不手工编辑导出物。

## 7. 执行顺序

1. 盘点 A01–A28 的职责、索引、模板、教学例和检查器夹具，建立系统级去重清单。
2. 编写 A29 规范，明确项目实例、系统级章节、权威边界、版本和 handoff 条件。
3. 编写 A29 模板和 FocusTask 教学例；教学例使用虚构系统和两个模块，不声明真实实现或验收结果。
4. 更新索引、目录、约定、总纲、README、A28 输入、A01/A04/A10 入口和相关活动指南。
5. 扩展检查器与测试，先通过静态正例，再验证所有 A29 反例诊断。
6. 更新制品关系图和图解说明，使用仓库外 Mermaid 工具重新导出并做视觉检查。
7. 更新执行状态和检查记录，运行全部检查，提交并推送三个远程。

每一步完成后保留可复查的文件差异和命令结果。中断后从执行状态、Git 差异和最后一个通过的检查点恢复，不重做已完成步骤。

## 8. 验收标准

| 编号 | 验收项 | 通过条件 |
| --- | --- | --- |
| AC-A29-01 | 制品完整 | A29 规范、模板、教学例存在，索引和目录均有 A29，检查器报告 29 类制品 |
| AC-A29-02 | 系统边界清楚 | 系统目标、范围、非目标、模块地图和外部关系可从 A29 直接找到 |
| AC-A29-03 | 模块地图可接续 | 每个当前模块有 A28 路径、版本、状态、职责和依赖；A28 能反向标记所属 A29 |
| AC-A29-04 | 跨模块流程可实施 | 关键旅程、模块顺序、数据流、失败传播和恢复边界有来源和下游入口 |
| AC-A29-05 | 去重与权威边界清楚 | A29 不承载 A01/A02/A03/A04/A28 的完整细节，冲突有来源、影响和待决定记录 |
| AC-A29-06 | 生成流程可执行 | A29-START/GENERATE/CLARIFY 能让 AI 先读资料、提问并生成带来源的草稿，不猜系统语义 |
| AC-A29-07 | 人工确认有效 | 确认人、确认版本、未决事项、复核意见和采纳决定可追踪 |
| AC-A29-08 | 可选复核受控 | 其他模型只读复核可选；建议与人工确认区分，不自动改变权威制品 |
| AC-A29-09 | Handoff 可恢复 | A29-HANDOFF 输出系统版本、模块/A28 清单、风险和下一条系统/模块指令 |
| AC-A29-10 | 工具与链接一致 | 文档检查、测试、图解导出和差异检查通过，无断链、无旧范围断言和无伪造证据 |

## 9. 检查命令与交付

在仓库根运行：

```sh
node scripts/check-docs.mjs
node --test scripts/check-docs.test.mjs
node scripts/render-model-diagrams.mjs --cli <mmdc> --puppeteer-config <config>
git diff --check
git status --short
```

预期结果：检查器 `artifacts: 29`、`activities: 4`、`stages: 3`、`errors: []`；所有测试通过；图解源码和导出文件存在且非空；无 Markdown 断链和空白错误。

完成后使用意图明确的提交信息提交，推送当前分支到 `origin`、`github`、`gitee`，并核对三个远程的当前分支指向同一提交。不得强制推送；失败时保留工作并报告失败远程、命令和恢复条件。

## 10. 失败恢复

- 资料不足：保留草稿和问题清单，等待开发人员回答；不把建议写成确认。
- A29 与 A01/A02/A04/A28 冲突：记录冲突和影响，按权威边界修订 A29 或请求正式变更，不覆盖专门制品。
- 模块缺少 A28：在模块地图中标记待生成，不能把系统摘要当作模块蓝图。
- 第二模型不可用：继续使用主模型和人工确认，不伪造独立复核结果。
- 检查器或图解工具失败：保留实际错误和已完成文件，先完成不依赖该工具的检查；不把缺输入诊断写成成功。
- 推送失败：保留本地提交和工作区，报告具体远程和命令，不强制推送。

## 11. 明确决策

- A29 正式纳入 A01–A29 制品体系。
- 一个项目对应一个 A29 当前实例。
- A29 标题为“系统蓝图”，基名为 `A29-system-blueprint`。
- A29 与 A28 使用 6 类专属操作：START、GENERATE、CLARIFY、REVIEW、REVISE、HANDOFF。
- 主模型负责资料整理和草稿生成，开发人员负责回答系统级问题和最终确认。
- 其他模型可以进行可选的独立只读复核；复核建议必须经过开发人员决定后才能修订。
- A29 是系统级 handoff 入口，不取代 A13 会话交接。
