# A20 维护演练报告

来源：CHANGE-001、DRILL-001、DRILL-002及已授权计划。真实问题修复与人为注入明确区分。

## 需求变更

v0.1→v0.2增加状态筛选，先写变更、功能规格与CF用例，再更新读接口和UI。第一次独立验收发现APP-06（创建中历史导航竞态），旧候选no-go；修复后新rc2构建、三引擎四视口CF-09与额外角色组合通过，独立复核允许本地发布。记录链：CHANGE-001→TASK-004→app实现→filter/performance测试→acceptance-v02→rc2清单与运行。

## 权限注入与修复

仅在.runtime/permission-drill副本移除Viewer拒绝，专用drill库和3213。独立验收者编写不依赖实现的AC-09脚本；其首次工具连接阻塞后由协调者执行原脚本，观察API201、任务3→4，尽管UI无按钮。恢复拒绝分支并重新构建后同脚本返回403、任务4→4、幂等记录不变。失败和修复JSON/截图、注入及修复哈希在maintenance/permission；正常服务源文件与修复后的权限服务一致，真实代码未注入。

验收判据与脚本由独立验证者建立；故障候选03:23与修复候选03:26由协调者执行，独立验收者03:34亲自执行修复候选通过，并已独立核对故障运行、修复运行、脚本/规格/候选摘要及数据保留链。详见[闭环独立复核](permission/initial-report.md)。03:34文件名虽为initial，摘要实际属于修复候选；独立验收者没有亲自完成故障初版请求，该次尝试在health阶段阻塞。准确结论是独立脚本检测、协调者故障执行、独立证据复核和独立修复实测，不冒充全部工具运行都由独立者完成。

## 失败发布与回退

坏包健康真实503，发布命令退出1；恢复v0.1后仍读到v0.2创建的数据，再恢复v0.2后也保留回退期间数据，Viewer拒绝和同键幂等不变。具体发布版本、检查点与运行结果见releases/v0.2.0-rc2/run.md。

## 备份恢复与故障处理

backup-restore实际将发布库导出到忽略目录，在新建的专用恢复库导入，Task.id列表完全一致，再只删除本次恢复临时库。备份文件仍可本地恢复，未删除原发布数据。不宣称达到30天保留或季度计划。

http-fault用单独真实Next HTTP进程连接停滞的虚构PG端点，health/login约5秒返回通用503。共享数据库未停止，不泄露凭据；不推导所有CPU阻塞或事务取消保证。

## 模型反馈

发现跨时区幂等、提交闭包竞态、浏览器缓存缺失、过时入口等具体问题。改进应分别回写契约/测试、交接状态、工具能力检查及候选绑定提示词。第一次失败保留，验证覆盖的边界如实说明，不能用一次通过宣称全场景可靠。

## FIX-002末轮修复

独立审查指出长缩放截图证据不足；补所有活动字段几何后发现真实遮挡。未放宽200%标准，改CSS让fieldset独立滚动、操作区占独立空间。rc3真实缩放和三引擎56项回归通过，独立复核后重新本地发布并重启，数据8→9→10且旧检查点保留。具体版本与运行见releases/v0.2.0-rc3/run；这是一次真实发现与修复，和预置DRILL-001区别记录。
