# A12/A15 v0.1 独立代码审查与修订

审查者：独立上下文Agent `/root/v01_review`；输入是仓库入口和handoff-v01，无原始聊天。只读代码与既有结果，未修改产品、未启动浏览器或写数据库。

候选边界：第一次发现后实现继续修订，审查返回时源码摘要为da4c71c4803170246f180c387f02899e19799a0fd7c1d4ea0ad942055f974893；不能将它视为之后全部变更的验收。

| 发现 | 预期/实际与复现 | 结论 | 修订与后续证据 |
| --- | --- | --- | --- |
| APP-01 | 今日截止日期成功但丢响应，跨午夜重试应返回首次201；旧实现先日期校验得到400 | 失败，静态和隔离探针 | 先规范化查已成功幂等记录，当前权限仍重验；新增跨日集成测试通过，首次10测试记录 |
| APP-02 | 表单打开后移除成员，应404并关闭/隐藏项目；旧UI仅403和归档分支关闭 | 失败，代码检查 | 404走关闭刷新；三浏览器permission-removal E2E实际通过 |
| APP-03 | 同一UUID大小写写法需共享幂等行为；原字符串锁不同，DB视为相同UUID | 失败，校验探针和并发推导 | ID/key与负责人规范小写；新增并发回归待运行 |
| APP-04 | 负责人检查和写入之间不应被并发移除 | 无法判定，风险窗口 | 写入前锁负责人关系行，待回归；不谎称原并发已复现 |
| APP-05 | 三浏览器截图应分别保存 | 失败，证据路径覆盖 | 截图路径加入browser，需重跑产出各自证据 |

其他实际E2E发现：桌面侧栏对比度4.41低于4.5，日期错误焦点未在解除disabled后恢复。已修订深色文本和渲染后聚焦；e2e-v01-fixed记录39项通过。

依赖检查：镜像审计404失败后用官方registry检查，发现7条汇总风险，见audit-official-v01。ADR-0004记录修补选择，旧候选不能发布。应用整体仍待修补后新版本全部检查与独立复核。

后续证据：unit-remediated-retry为11条数据库/单元通过，unit-complete-v01包含独立接手3项共14通过；e2e-remediated中Chromium/Firefox通过，WebKit共享缓存缺失标失败后用项目缓存补跑webkit-local-v01，13/13通过。audit-remediated为0漏洞，build-remediated构建通过。候选产品文件摘要在releases/v0.1.0/source-files.json，产物在artifact.json；暂待最终只读复核。
