# A26 同类问题排查与修复记录 — FocusTask 教学示例

> 教学填写示例。用于演示文档内容与检查方法，不作通过结论。

## 元信息

- 实例ID：FocusTask-A26-teaching
- 版本：teaching-1
- 状态：草稿（教学）
- 责任角色：审查责任人
- 输入：[FocusTask 教学上下文](../context.md)，版本 teaching-1。
- 确认来源：教学设定；实际项目需另行确认。
- 下游用途：修复执行者、审查责任人、维护者与A22改进输入。
- 生成方式：按制品模板填写的教学例。

## 问题与复现

虚构问题：Viewer 直接调用创建接口写入成功，预期 403 且无数据变化；候选 teaching-candidate-01（演示标识）；位置为示例服务端创建流程；复现依赖运行环境，本例未执行。

## 根因与模式

根因：写请求处理前未重新校验成员角色与项目状态。可重复模式：仅依赖页面隐藏入口、沿用会话中缓存的角色、重试路径跳过授权、只校验登录不校验操作权限。

## 排查范围与方法

方法：按调用路径检索写操作入口，检查授权中间件结构，并按业务条件（写操作＋角色＋项目状态）做语义检索，不局限相同文本。覆盖创建、筛选保存、归档切换与只读接口；未检查生成文件与第三方依赖，原因：非自有实现。

## 实例分类

已确认 2 处（创建、筛选保存）；疑似 1 处（归档切换，待运行验证）；不适用 3 处（只读接口，无写入）。分类依据为授权检查位置与业务条件，不依据文本相似度。

## 修复与回归

修复：写路径统一在服务端重新授权并检查项目状态；回归：AC-09、AC-10 与幂等重试用例，另加归档项目写入用例；运行验证未执行。

## 复查与规则沉淀

再次排查：静态复查完成，运行复查阻塞（无环境）。规则沉淀到 A08（写请求必须重新授权）与 A16（相关证据标为待重验）；剩余风险：疑似项未验证。

## 检查与交接

运行类检查：未执行。实际采用时替换项目输入并验证。

下一步：使用 [A26 操作指南](../../../../model/artifacts/A26-pattern-remediation.md) 的 GENERATE 完成排查与修复，再由 VERIFY 复核回归。业务语义变化未确认时用 CLARIFY，不按相似度直接替换代码。

## 填写记录样例（教学）

| 实例 | 位置（虚构） | 分类 | 依据 | 处置 |
| --- | --- | --- | --- | --- |
| P-01 | 创建任务写路径 | 已确认 | 仅校验登录，未校验角色 | 修复并回归，运行未执行 |
| P-02 | 筛选保存写路径 | 已确认 | 沿用会话缓存角色 | 修复并回归，运行未执行 |
| P-03 | 归档切换写路径 | 疑似 | 授权位置不明，需运行验证 | 待验证，暂不修改 |
| P-04 至 P-06 | 只读查询接口 | 不适用 | 无写入与状态变化 | 记录依据，不修改 |

证据：本表描述虚构代码片段与判定方式，不是仓库实现结果；全部覆盖不等于证明不存在其他缺陷。
