# A24 工程化差距与重构方案 — FocusTask 教学示例

> 教学填写示例。用于演示文档内容与检查方法，不作通过结论。

## 元信息

- 实例ID：FocusTask-A24-teaching
- 版本：teaching-1
- 状态：草稿（教学）
- 责任角色：技术责任人
- 输入：[FocusTask 教学上下文](../context.md)，版本 teaching-1。
- 确认来源：教学设定；实际项目需另行确认。
- 下游用途：实施重构的执行者、审查责任人、独立验收者。
- 生成方式：按制品模板填写的教学例。

## 当前与目标状态

教学架构为 Next.js/React/TypeScript 与 PostgreSQL/Prisma（目标项目设定，本仓库未安装）；目标：权限、项目状态与幂等校验集中在服务端并与写入同事务。

## 差距清单

G-01 页面层判断角色即视为授权（影响 AC-09）；G-02 幂等记录与任务非同事务（影响 AC-08）；G-03 筛选参数缺枚举校验（影响 teaching-filter 的 400 语义）。

## 策略与步骤

G-01、G-02 调整为服务端统一校验；G-03 保留页面初检并新增服务端校验。步骤先 G-02 后 G-01，回退点为上一可运行教学版本。

## 行为映射与验证

AC-08 映射到服务端创建流程与幂等记录；回归为幂等重试与角色直连写入用例；验证方式已列出，均未执行。

## 风险与失败恢复

风险：事务边界调整影响并发写入；恢复：回退到 teaching-base-01 并核对数据一致性；需人决定：是否整体替换数据访问层。

## 检查与交接

运行类检查：未执行。实际采用时替换项目输入并验证。

下一步：使用 [A24 操作指南](../../../../model/artifacts/A24-refactoring-plan.md) 的 START/GENERATE，再由 REVIEW/VERIFY 检查实际结果。技术取舍未确认时用 CLARIFY 或 SELECT 记录来源。

## 填写记录样例（教学）

| 差距 | 领域 | 影响 | 策略 | 验证状态 |
| --- | --- | --- | --- | --- |
| G-01 | 权限 | AC-09 可能被绕过 | 调整：服务端重新授权 | 未执行 |
| G-02 | 数据一致性 | AC-08 可能重复写入 | 调整：同事务写入 | 未执行 |
| G-03 | 接口契约 | 非法筛选值行为未定 | 保留页面初检＋新增服务端校验 | 未执行 |

证据：本表描述虚构教学架构，不是仓库实现结果，也不代表已确认的技术选型。
