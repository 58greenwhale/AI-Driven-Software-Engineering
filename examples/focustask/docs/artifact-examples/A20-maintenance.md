# A20 维护验证报告 — FocusTask 教学示例

> 教学填写示例，不是实际验收、部署或人工确认记录。真实证据在 evidence 中另行产生；本文件不得用来宣称运行通过。

完整的真实项目实例见 [A20 实证实例](../../evidence/maintenance/report.md)；实际使用及证据关系见 [制品使用表](../../evidence/artifact-usage.md)。本教学文件保留简化结构，真实实例展示填充深度与版本/证据要求。

## 元信息

- 实例ID：FocusTask-A20-teaching
- 版本：0.1
- 状态：草稿（教学）
- 责任角色：维护责任人
- 输入：已存在的 FocusTask 规格及实施计划 1.1；不虚构未生成的代码版本。
- 确认来源：项目范围沿用计划，本文是解释结构的示例。
- 下游用途：问题责任人、下一位维护者和模型评价。
- 生成方式：示例内容，由规范作者撰写；不是实际用户逐条输入。

## 事件与边界

DRILL-001 人为权限缺陷，隔离副本，不是真实生产事故。

## 诊断与决定

验证服务入口是否重新校验角色，不凭UI推断权限安全。

## 修订与追踪

权限规则不变，修复实现；旧证据保留，新候选重新测试。

## 回归与再发布

未运行时无法关闭；复用同AC再验证数据库无写入。

## 关闭与改进

记录指南是否遗漏服务端权限检查，并修订提示词。

## 检查与交接

运行类检查：未执行，本教学示例不作通过结论。内容需按实际项目输入替换后验证。

下一步：使用 [A20 操作指南](../../../../model/artifacts/A20-maintenance.md) 的 START/GENERATE，再由 REVIEW/VERIFY 检查实际结果。涉及关键业务规则未确认时用 CLARIFY，不跳过来源。

## 填写维护案例（虚构教学）

事件：DRILL-teaching-01，人为放宽Viewer权限的隔离演练。期望：403且任务数量不变。实际运行：尚未开始，不填201或通过结果。

| 步骤 | 计划和判断 | 当前事实 |
| --- | --- | --- |
| 复现 | 用同一AC直调API并比较数量 | 缺运行候选，阻塞 |
| 修复 | 若确认权限缺失，恢复服务端角色检查，不改Viewer只读规格 | 尚未修改源码 |
| 回归 | 原用例与其他角色/归档/幂等验证 | 尚未执行 |
| 关闭 | 新候选验收与再发布、数据验证齐全 | 未关闭 |

这展示如何诚实记录未完成维护；实证需附独立验证者的失败和修复后结果。
