# A25 面向人的实现解读 — FocusTask 教学示例

> 教学填写示例。用于演示文档内容与检查方法，不作通过结论。

## 元信息

- 实例ID：FocusTask-A25-teaching
- 版本：teaching-1
- 状态：草稿（教学）
- 责任角色：审查责任人
- 输入：[FocusTask 教学上下文](../context.md)，版本 teaching-1。
- 确认来源：教学设定；实际项目需另行确认。
- 下游用途：产品与技术责任人分层审阅、后续维护者、独立验收者。
- 生成方式：按制品模板填写的教学例。

## 解读范围与代码版本

覆盖创建任务的页面表单与服务端流程（教学代码描述，非仓库实现）；代码版本 teaching-1；数据库迁移与第三方依赖未覆盖，原因：教学上下文未提供实现。

## 模块关系与数据流

页面表单初检 → 路由处理 → 服务端创建流程（身份、成员资格、项目状态、字段校验）→ 事务写入任务与幂等记录 → 返回服务端 Task → 列表按 updatedAt 倒序刷新。

## 规格到实现的映射

AC-03、AC-04 映射到字段校验；AC-07、AC-08 映射到幂等键作用域与事务；AC-09、AC-10 映射到授权与项目存在性处理；AC-13 映射到表单焦点与响应式（未验证）。

## 重要判断与设计理由

status 由服务端固定为 todo，客户端不可写；日期按项目时区 Asia/Shanghai 判定，不按 UTC 改写；重试保持原幂等键。理由来自教学上下文的一致性要求。

## 风险与需人决定的问题

创建成功但不符合当前筛选时的提示措辞是否构成行为变化，需产品责任人确认；未确认前按表现变化处理。

## 分层审阅入口

先读模块关系与数据流，再按 AC 下钻到校验与事务；追问起点：权限校验位置、幂等键作用域、错误响应是否泄露项目存在性。

## 检查与交接

运行类检查：未执行。实际采用时替换项目输入并验证。

下一步：使用 [A25 操作指南](../../../../model/artifacts/A25-implementation-guide.md) 的 START/GENERATE，再由 REVIEW/VERIFY 抽样核对映射。代码变化后用 REVISE 同步解读与覆盖记录。

## 填写记录样例（教学）

| 规格条目 | 教学实现位置 | 教学测试位置 | 结论来源 |
| --- | --- | --- | --- |
| AC-08 幂等重试 | 服务端创建流程与幂等记录 | 幂等重试用例 | 静态阅读；运行未执行 |
| AC-09 Viewer 禁止创建 | 服务端授权检查 | 角色直连写入用例 | 静态阅读；运行未执行 |
| AC-13 移动端与键盘 | 表单焦点与布局 | 浏览器用例 | 未覆盖，浏览器验证未执行 |

证据：表中位置为虚构教学描述，不是仓库文件路径或已执行结果。
