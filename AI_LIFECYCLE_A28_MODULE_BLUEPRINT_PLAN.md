# A28 模块蓝图建设计划

> 计划版本：1.0 · 目标模型版本：0.4 · 状态：待执行
>
> 用途：为每个业务模块建立一份可供 AI 实现原型的模块级上下文，并把模块全景、功能点和现有制品连接成可恢复的 handoff 入口。

## 1. 目标与背景

当前制品体系已经覆盖项目范围（A01）、领域与权限（A02）、单项功能规格（A03）、架构与契约（A04）以及实施计划（A10），但缺少模块级上下文。对于订单、支付、履约等包含多个功能点的业务模块，如果直接逐项编写 A03，AI 需要在多个文档之间自行重建模块边界、跨功能关系和整体流程，容易出现重复定义、遗漏依赖或局部正确但整体冲突。

本计划新增 A28“模块蓝图”，一个业务模块对应一个 A28 实例。A28 先由 AI 根据现有资料整理草稿，开发人员回答关键问题并确认业务边界；确认后形成 Markdown 制品，作为后续原型构建和会话 handoff 的模块级入口。条件允许时，可由不同模型独立 review 并提出 revise 建议，但多模型复核是可选质量环节，不是生成或交接的硬性前置。

整体关系：

```text
项目目标与范围（A01）
  -> 模块蓝图（A28）
  -> 领域与权限规则（A02）/架构与契约（A04）
  -> 功能点规格（多个 A03）
  -> 设计与实施计划（A10）
  -> 原型构建与后续活动
```

## 2. 范围与边界

### 2.1 范围内

- 新增 A28 规范、空白模板和 FocusTask 教学填写示例。
- 将 A28 纳入 `model/artifact-index.json`、`model/artifact-catalog.md`、`model/conventions.md` 和文档检查器。
- 将现有制品范围从 A01–A27 扩展为 A01–A28，保持 A01–A27 的编号、路径和语义稳定。
- 在总纲、README、活动指南、提示词协议和相关制品中说明 A28 的输入、输出和使用入口。
- 更新 `model/diagrams/artifacts.mmd`，表达 A28 与 A01、A02、A03、A04、A10、A13 的关系，并重新生成对应 SVG/PNG。
- 增加 A28 的生成、澄清、复核、修订和交接提示词，以及针对性的静态检查和反例测试。

### 2.2 范围外

- 不创建真实订单、支付或其他应用代码、数据库、接口服务或运行证据。
- 不把 A28 变成新的项目管理系统、工作流引擎或自动裁剪机制。
- 不在 A28 中复制 A02 的完整权限矩阵、A03 的完整验收用例、A04 的完整接口契约或 A10 的实施排期。
- 不要求必须使用第二个模型复核，也不把第二个模型的建议自动写成开发人员确认。
- 不重写 A01–A27 的历史结论；只同步必要的范围、链接和下游入口。

## 3. 权威边界与去重规则

A28 是模块级摘要、索引和关系图，不是其他制品的替代品。实现者读取 A28 建立整体上下文，再按功能和风险读取权威细节。

| 内容 | 权威制品 | A28 中允许保留的内容 |
| --- | --- | --- |
| 项目愿景、版本范围、项目级成功条件 | A01 | 模块目标与 A01 版本引用 |
| 术语、实体、状态、权限、一致性和时间规则 | A02 | 模块级概念摘要、关键不变量和 A02 引用 |
| 单项功能流程、输入、异常、AC 和测试映射 | A03 | 功能清单、功能关系、A03 路径和状态 |
| 技术组件、接口契约、依赖方向和非功能约束 | A04 | 模块边界、依赖摘要、接口/事件入口和 A04 引用 |
| 本轮任务、活动裁剪、顺序、并行和集成条件 | A10 | 当前模块实施入口和 A10 版本引用 |
| 验证类型、风险和证据要求 | A07/A14/A16 | 模块级风险和待重验入口 |
| 会话接手、版本和下一条指令 | A13 | 模块交接摘要；会话级状态仍由 A13 负责 |

发生冲突时，A28 标记冲突来源、影响和待决定事项，不能覆盖 A02/A03/A04 等权威制品。权威制品更新后，A28 的关联版本和摘要必须同步。

## 4. A28 文档结构

文件基名为 `A28-module-blueprint`，三类文件分别位于：

- `model/artifacts/A28-module-blueprint.md`
- `model/templates/A28-module-blueprint.md`
- `examples/focustask/docs/artifact-examples/A28-module-blueprint.md`

规范和模板必须包含以下章节：

1. 用途与生命周期：模块级触发条件、生产者、责任角色、输入、使用者、输出和关联制品。
2. 模块定位与边界：模块目标、职责、所属业务上下文、包含范围、非目标和与项目 A01 的关系。
3. 能力地图与功能清单：功能编号、名称、优先级、状态、A03 路径、依赖、前置条件和完成关系。
4. 概念领域模型：核心实体、关系、生命周期、模块内不变量和跨功能约束；详细规则引用 A02。
5. 角色与端到端流程：角色职责、主流程、关键分支、跨功能流程和外部参与者摘要。
6. 模块关系与数据流：依赖模块、接口、事件、数据所有权、读写边界、外部系统和失败传播。
7. UI/API/数据约束摘要：当前已确认的入口、状态、错误语义、数据边界和非功能约束；详细契约引用 A04/A05/A07。
8. 原型边界与假设：真实行为、模拟行为、暂不实现项、未确认交互、数据夹具和解除条件。
9. 关联制品与版本：A01、A02、A03 清单、A04、A07、A10、A13、A16 的路径、版本、状态和用途。
10. 未决事项与变更影响：问题、来源、影响范围、责任人、决定状态和下一步。
11. 确认与 handoff：开发人员确认范围、确认版本、复核意见、剩余风险、接手入口和下一条原型构建指令。

每个模块实例必须有稳定的模块 ID，例如 `orders`；每个功能点的 A03 必须记录所属模块 ID 和 A28 路径。A28 的版本变化保留旧版本关系，不覆盖历史确认。

建议的实际项目布局：

```text
docs/modules/orders/
  module-blueprint.md
  features/
    create-order.md
    pay-order.md
    cancel-order.md
```

其中 `module-blueprint.md` 是 A28 实例，`features/` 下的文件是该模块的 A03 实例。实际项目可以沿用已有目录约定，但必须能从 A28 找到所有当前功能点规格。

## 5. 生成、确认、复核与交接

A28 不复制 A01–A27 的完整九类操作链，采用面向模块蓝图的六类操作：

### A28-START：盘点模块资料

输入为模块目标、现有产品资料、A01/A02/A04/A10、已有 A03、代码事实和相关外部模块。AI 先列出已知事实、缺口、冲突、候选功能点和需要开发人员回答的问题，不自行补充业务规则。

### A28-GENERATE：整理模块蓝图

AI 按本规范和模板生成草稿，建立模块边界、能力地图、概念模型摘要、跨模块关系、原型假设和关联制品索引。每项事实记录来源；无法确认的内容标为待确认、模拟或不适用。

### A28-CLARIFY：收集开发人员回答

当资料不足或存在会改变范围/业务语义的冲突时，AI 提出按影响排序的问题和选项。开发人员回答后，记录回答来源、适用范围、影响的章节和需要同步的 A02/A03/A04/A10。可发现的仓库事实直接核对，不重复询问。

### A28-REVIEW：独立复核蓝图

开发人员可以自行复核，也可以在条件允许时交给其他模型只读复核。复核检查模块边界、功能覆盖、关系完整性、来源一致性、重复定义、未决项和下游可实施性。复核者不能修改权威制品或把建议直接写成确认。

### A28-REVISE：按确认结果修订

开发人员决定哪些复核意见被采纳。AI 先列影响，再修订获准内容，更新关联制品版本和失效/待重验项；业务语义没有确认来源时暂停相关改动。

### A28-HANDOFF：交付模块上下文

输出人工确认后的 Markdown、模块版本、关联 A03 清单、权威制品版本、未决事项、剩余风险和下一条原型构建指令。A28 handoff 交付模块上下文，不替代跨会话的 A13。

## 6. 检查器、索引和图解改动

- `model/artifact-index.json` 追加 A28，沿用 `id`、`title`、`spec`、`template`、`example` 字段。
- `model/artifact-catalog.md` 追加 A28 导航行，说明模块级触发、输出位置和下游用途。
- `model/conventions.md` 将制品范围扩展到 A01–A28，并说明模块实例 ID、版本和 A28 与 A03 的关联规则。
- `scripts/check-docs.mjs` 将期望制品扩展到 28；A01–A27 继续检查 9 类提示词，A28 检查 `START`、`GENERATE`、`CLARIFY`、`REVIEW`、`REVISE`、`HANDOFF` 6 类提示词。
- `scripts/check-docs.test.mjs` 增加 A28 正例和反例：缺规范、缺模板、缺教学例、缺任一 A28 提示词、重复/越界编号、A28 链接断裂和教学边界缺失。
- 更新 README、总纲、相关活动指南和 A03/A10 入口说明，使 AI 先读取所属 A28，再进入功能点和实施计划。
- 更新 `model/diagrams/artifacts.mmd` 表达 `A28 -> A02/A03/A04/A10` 的模块上下文关系；只修改 Mermaid 源码后重新导出对应 SVG/PNG，不手工编辑导出物。

## 7. 执行顺序

1. 盘点 A01–A27 的职责、索引、模板、教学例和检查器夹具，建立 A28 的去重清单。
2. 编写 A28 规范，明确章节、权威边界、模块 ID、版本和 handoff 条件。
3. 编写 A28 模板和 FocusTask 教学例；教学例使用虚构订单模块，不声明真实实现或验收结果。
4. 更新索引、目录、约定、总纲、README、A03/A10 入口和相关活动指南。
5. 扩展检查器与测试，先通过静态正例，再验证所有 A28 反例诊断。
6. 更新制品关系图和图解说明，使用仓库外 Mermaid 工具重新导出并做视觉检查。
7. 更新执行状态和检查记录，运行全部检查，提交并推送三个远程。

每一步完成后保留可复查的文件差异和命令结果。中断后从执行状态、Git 差异和最后一个通过的检查点恢复，不重做已完成步骤。

## 8. 验收标准

| 编号 | 验收项 | 通过条件 |
| --- | --- | --- |
| AC-A28-01 | 制品完整 | A28 规范、模板、教学例存在，索引和目录均有 A28，检查器报告 28 类制品 |
| AC-A28-02 | 模块边界清楚 | 目标、职责、范围、非目标、功能地图和跨模块关系可从 A28 直接找到 |
| AC-A28-03 | 领域关系可实施 | 实体、生命周期和跨功能不变量有摘要、来源和 A02 入口，不重复制造第二份权威规则 |
| AC-A28-04 | 功能点可接续 | 每个当前功能点有 A03 路径、状态、依赖和优先级；A03 能反向标记所属 A28 |
| AC-A28-05 | 生成流程可执行 | A28-START/GENERATE/CLARIFY 能让 AI 先读资料、提问并生成带来源的草稿，不猜业务 |
| AC-A28-06 | 人工确认有效 | 确认人、确认版本、未决事项、复核意见和采纳决定可追踪 |
| AC-A28-07 | 可选复核受控 | 其他模型只读复核可选；建议与人工确认区分，不自动改变权威制品 |
| AC-A28-08 | Handoff 可恢复 | A28-HANDOFF 输出模块版本、关联制品、A03 清单、风险和下一条原型构建指令 |
| AC-A28-09 | 去重与冲突处理 | A28 不承载 A02/A03/A04/A10 的完整细节，冲突有来源、影响和待决定记录 |
| AC-A28-10 | 工具与链接一致 | 文档检查、测试、图解导出和差异检查通过，无断链、无旧范围断言和无伪造证据 |

## 9. 检查命令与交付

在仓库根运行：

```sh
node scripts/check-docs.mjs
node --test scripts/check-docs.test.mjs
node scripts/render-model-diagrams.mjs --cli <mmdc> --puppeteer-config <config>
git diff --check
git status --short
```

预期结果：检查器 `artifacts: 28`、`activities: 4`、`stages: 3`、`errors: []`；所有测试通过；图解源码和导出文件存在且非空；无 Markdown 断链和空白错误。

完成后使用意图明确的提交信息提交，推送当前分支到 `origin`、`github`、`gitee`，并核对三个远程的当前分支指向同一提交。不得强制推送；失败时保留工作并报告失败远程、命令和恢复条件。

## 10. 失败恢复

- 资料不足：保留草稿和问题清单，等待开发人员回答；不把建议写成确认。
- A28 与 A02/A03/A04 冲突：记录冲突和影响，按权威边界修订 A28 或请求正式变更，不覆盖专门制品。
- 功能点缺少 A03：在功能地图中标记待生成，不能把模块摘要当作功能规格。
- 第二模型不可用：继续使用主模型和人工确认，不伪造独立复核结果。
- 检查器或图解工具失败：保留实际错误和已完成文件，先完成不依赖该工具的检查；不把缺输入诊断写成成功。
- 推送失败：保留本地提交和工作区，报告具体远程和命令，不强制推送。

## 11. 明确决策

- A28 正式纳入 A01–A28 制品体系。
- 一个业务模块对应一个 A28 实例。
- A28 标题为“模块蓝图”，基名为 `A28-module-blueprint`。
- A28 采用 6 类专属操作：START、GENERATE、CLARIFY、REVIEW、REVISE、HANDOFF。
- 主模型负责资料整理和草稿生成，开发人员负责回答关键问题和最终确认。
- 其他模型可以进行可选的独立只读复核；复核建议必须经过开发人员决定后才能修订。
- A28 是模块级 handoff 入口，不取代 A13 会话交接。
