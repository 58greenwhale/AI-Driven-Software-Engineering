# 仓库清理执行记录

依据：[清理计划](../AI_LIFECYCLE_V03_CLEANUP_PLAN.md)。日期：2026-09-19。本轮不执行完整 v0.3 建设。

## 提交与范围

- 清理前提交：`67abd03fc58a7ffd9e3604c641703a0bc3eeef37`，`chore: snapshot repository before lifecycle cleanup`，分支 master。
- 提交包含 1,188 个文件；提交后工作区与快照一致，未提交忽略文件，未推送。
- [逐路径清单](cleanup-files.json)列出快照中的待删和保留文件，以及不能通过 Git 恢复的忽略目录。新增记录不属于旧内容删除集合。
- 提交前文档检查：174 份 Markdown、547 个本地链接，错误 0；旧证据审计：10,715 个摘要、91 份运行记录，失败 0。这里只说明清理前检查，不作为新模型实证。
- 候选文件名、私钥/令牌/凭据 URL 模式及已有本地敏感值扫描未发现问题。暂存空白检查报告 101 条旧 evidence 日志/文件格式告警，为保留原始快照未改写；不是敏感信息或提交失败。
- FocusTask 3210–3213、55461 无监听，旧 PostgreSQL 数据目录没有打开句柄；用户 PostgreSQL PID 933 监听 5432，目录在仓库外，未操作。删除前再次核对。

## 任务状态

| 任务 | 状态 | 依据与下一步 |
| --- | --- | --- |
| CLEAN-01 | 完成 | 提交成功并核对全部可入库文件 |
| CLEAN-02 | 完成 | 教学上下文已提取，22 份示例及通用制品已解除旧实证依赖 |
| CLEAN-03 | 完成 | 清单内 1,096 个文件及五类忽略目录/文件已删除，无进程需要停止 |
| CLEAN-04 | 完成 | 修订计划从 463 行精简为 327 行；工具与引用已校正，28 项首轮测试通过 |
| CLEAN-05 | 完成 | 残留已修复并复验，文档检查、28 项测试和范围/语义检查通过 |

## 恢复入口

清理已完成，不再创建提交或重复删除。下一任务是 v0.3 修订计划的 V03-01，尚未启动。忽略的运行数据已删除，不能从 Git 恢复；没有另建备份。后续工作先核对 Git 差异，不恢复旧内容来修复链接。

## 实际删除

逐路径清单中的 1,096 个文件删除前逐个计算 Git blob 摘要，与快照全部一致。保留集合为快照中的 92 个文件，后续只做范围内修订；新增教学上下文、清理记录/清单和工具测试另行检查。

忽略内容删除前统计（普通文件逻辑字节，不含符号链接）：`.next` 2,130 文件/176,949,094 字节；`.runtime` 22,412 文件/1,084,541,261 字节；`node_modules` 28,655 文件/746,235,978 字节；`test-results` 1 文件/45 字节；`tsconfig.tsbuildinfo` 1 文件/144,015 字节。合计 2,007,870,393 字节，包含本地测试数据库、浏览器、构建和原运行配置，无 Git 恢复途径。

71 个符号链接未被跟随。删除前再次确认任务端口无监听、数据库目录无打开句柄，未发送停止信号；应用与实证目录已不存在，`.git` 保留。仓库外工具、共享缓存和用户数据库未操作。

首轮范围验收发现旧 `docs/` 仅残留被忽略的 `.DS_Store`（14,340 字节）。已核对为 Finder 元数据、补入清单并删除该文件及空目录；未删除根目录无关元数据。初次范围检查的此项失败已修复，最终复验通过。

## 验收

本轮为执行者自检与静态验收，没有另行开展独立应用验收。以下全部针对清理后工作区，不沿用清理前测试结论。

最终检查候选：94 个保留或新增的非忽略文件，聚合 SHA-256 为 `af60de45b68514616919b9dd8e190a28835af7c511fbd66da43d14aad8e06aba`。对现存的已跟踪及非忽略未跟踪路径去重、排序，生成 `[路径, 文件字节SHA-256]` 数组，再对其 `JSON.stringify` 结果计算 SHA-256。仅排除本记录和 `execution/status.md` 两份结果文档，避免摘要自引用；两者仍纳入文档检查。当前共 96 个可入库文件。

| 验收项 | 结果 | 实际依据 |
| --- | --- | --- |
| 提交前置 | 通过 | HEAD 为清理前提交；此前 1,188 文件与快照一致；删除前逐个比较目标 blob |
| 删除范围 | 通过 | Git 删除集合与清单 1,096 项完全一致，无额外或缺失删除，保留的 92 项均存在 |
| 忽略内容 | 通过 | 清单六项忽略目标均不存在；旧 docs、app、evidence、baseline 目录均不存在 |
| 教学独立 | 通过 | 22 份教学例均引用 context，保留教学/不作通过标记；无旧应用、实证或旧计划输入 |
| 文档结构 | 通过 | `node scripts/check-docs.mjs` 退出 0：89 份 Markdown、250 个本地链接、22 类制品、3 份阶段指南，错误 0 |
| 校验器 | 通过 | `node --test scripts/check-docs.test.mjs` 退出 0，28 项通过、0 失败、0 跳过；缺文件等反例有诊断，不是忽略错误 |
| 渲染诊断 | 通过 | `node scripts/render-model-diagrams.mjs` 预期退出 1，明确缺四份新版源码；未调用外部工具、未生成图片，不计图解完成 |
| 计划完整性 | 通过 | 修订计划 463→327 行，保留四项活动、A01–A27 目标、7 任务、10 标准、12 场景；已校正清理后输入 |
| 当前状态 | 通过 | 未创建四份活动指南、A23–A27 或新图解；后续任务仍未执行，未将清理算作模型实证 |
| 服务与 Git | 通过 | 用户 PostgreSQL 仍为 PID 933/5432；未发停止信号；本轮只有一次提交，暂存区空，未推送 |

另外，`git diff --check` 退出 0。最终范围检查的 16 个布尔项全部为 true：删除集合/物理缺失/保留存在、无第二提交、暂存区空、仅四个预期新文件、无旧目录、教学目录白名单、22 制品、教学边界、7/10/12 数量、无提前交付、无旧输入、渲染诊断。

语义走查：README 可到达当前修订计划与状态；制品目录的 22 条路径均可到规范、模板和教学例；A03/A07/A14 可仅凭 context 理解输入、规则与未执行结论；所有保留指南不再要求旧计划、旧应用或旧实证。规范中的目标项目路径仍是示例，不要求在本仓库存在。

## 交付

已完成仓库清理。新增文件仅为教学上下文、本记录、逐路径清单和校验器测试。改写最新计划、导航、根/教学规则及受影响规范/示例/工具；未改写空白模板的无关结构。删除源码和文档可从清理前提交恢复；已删除忽略数据不能通过 Git 恢复。

清理结束时改动留在工作区，该阶段没有第二次提交或推送。用户随后明确授权提交并推送，作为独立的交付操作；上述清理阶段检查保留其当时含义。模型下一步仍需另行启动 `AI_LIFECYCLE_V03_REFINEMENT_PLAN.md`，四项详细活动指南、A23–A27、新图解及新增应用实证未完成。
