# VIS-001 生命周期模型图解交付

来源：用户要求“将这个生命周期模型用合适的图表达出来”。这是原生命周期实施计划完成后的文档扩展，不改变阶段、业务规格或应用。

## 产物

- [图解入口](../model/visual-guide.md)：读图说明、阶段与制品链接。
- [生命周期总览](../model/diagrams/lifecycle.svg)：人驱动、首次准备、六阶段和反馈路径。
- [协作时序](../model/diagrams/collaboration.svg)：人、执行 AI、工具/仓库与独立验证 AI 的交互和失败修订。
- [制品流转](../model/diagrams/artifacts.svg)：A01–A22 的主要使用位置与跨阶段关系。
- 同目录 `.mmd` 为可编辑源，`.png` 为高清导出；渲染配置与 `scripts/render-model-diagrams.mjs` 支持重新生成。

README 和 model/overview 已接入图解，overview 的旧内嵌草图替换为同源 SVG，减少重复维护。

## 检查与实际结果

使用 Mermaid CLI 11.15.0 和本机 Chrome 实际导出三份 SVG、三份 PNG，命令退出0。CLI安装在独立临时工具目录，没有修改应用依赖。

| 图 | PNG 像素 | SVG 文字元素 | 超出画布的文字 | 可访问标题/描述 |
| --- | --- | --- | --- | --- |
| 生命周期总览 | 4806×1592 | 27 | 0 | 有 |
| 协作时序 | 2906×2992 | 32 | 0 | 有 |
| 制品流转 | 4754×1536 | 23 | 0 | 有 |

浏览器实际读取 SVG，核对文字矩形与画布边界、必需阶段/角色文本及 A01–A22，未发现遗漏；三份 SVG 均不依赖 foreignObject HTML 标签。目视检查中文、箭头、条件分支和图例，未见裁切或遮挡。

`node scripts/check-docs.mjs` 通过：172 份活动 Markdown、538 个本地链接、22 类制品、六阶段，0错误。`git diff --check` 与渲染脚本语法检查通过。本次仅文档及导出图变更，未重跑不受影响的应用测试。

## 语义与局限

图表达已确认模型，不代表额外验证结果。保持首次准备非每轮必做、已授权连续执行、独立验证不修改产品、条件制品按需使用和受影响内容才重验。A06/A13/A15/A16/A21/A22贯穿阶段，A07提前定义判据。

PNG便于分享，SVG可缩放；中文SVG在不同系统依赖当地字体，缺少列出的中文字体时可使用已渲染PNG。长横向图可在图解中打开高清文件放大查看。原实证报告/校验记录保持历史含义；本次新文档数量以本报告为准。尚未提交或推送。
