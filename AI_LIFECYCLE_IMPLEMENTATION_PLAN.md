# 人驱动 AI 的软件生命周期模型：实施与实证计划

> 版本：1.1 · 日期：2026-09-19
>
> 用途：作为后续 goal 执行的任务依据。本文描述待完成工作，不代表模型、应用或实证已经完成。
>
> 修订依据：用户明确要求“不用 docker，发布和维护可以直接在本地操作”。改为独立本地进程与数据库，部署、维护、回退及证据要求不变。

## 1. 目标、范围与完成定义

将本仓库建设为一套**人驱动 AI 的软件生命周期模型**，覆盖需求分析、设计、实现、验证、发布和维护，并用可运行的 FocusTask 在本地模拟环境验证。

核心成果必须同时包含：

1. 生命周期模型：阶段、迭代与返工路径、人和 AI 的职责、进入与退出条件。
2. 制品体系：每阶段输出、下游用途、详细内容要求、模板、版本与更新规则。
3. 操作指南：人如何逐步驱动 AI 完成工作；每个人机交互步骤都有可直接使用的示例提示词。
4. 实证项目：真实应用、测试、构建、部署、变更、缺陷修复、回退和会话接手记录。
5. 模型评价：哪些制品被使用、是否达到预期、哪里需要返工，以及依据实证作出的模型修订。

模型适用于个人、小团队和大型团队；用明确的裁剪规则表达差异。一个小项目只能提供初步实证，不据此宣称大型团队适用性已经得到验证。

### 边界

- 起点为已有问题与目标输入，包含将模糊需求澄清为可验证规格；不展开市场调研、商业可行性和产品增长方法。
- 包含技术反馈驱动的维护：问题复现、影响分析、变更、修复、回归与重新发布。
- 发布与维护只在隔离的本地模拟环境执行；不操作真实生产环境。
- 不开发与实证无关的完整任务管理产品，不创建独立模型平台或提示词管理系统。
- 本次实施默认只产生本地文件、测试数据和必要的本地版本记录。远端推送、外部 PR、真实基础设施和付费服务不是完成前提。

### 不可降低的完成标准

“规范已写完”“模板齐全”“应用能启动”均不能单独代表目标完成。五项核心成果全部完成且必要实证通过，才能报告整体完成；环境或人工决定阻塞时必须保留未完成项。

## 2. 已确认原则与执行默认值

### 2.1 人驱动 AI 的协作循环

```text
人给出目标和约束
  -> AI 检查上下文、澄清问题、提出方案
  -> 人确认必要取舍，或 AI 依据既有授权继续
  -> AI 执行并产生制品
  -> AI 依据确认标准验证、解释证据
  -> 人确认、纠正或要求修订
  -> 交接到下一阶段，或返回受影响阶段
```

所有阶段遵循上述原则。审阅、决策分析、风险评估、部署操作和缺陷调查都提供 AI 辅助方式，不要求人独自撰写任何制品。

人承担目标、关键业务规则、重要风险取舍和发布范围的责任；AI 在已授权范围内连续执行，不为命名、排版、常规检查等事项逐项索取批准。已有用户确认持续有效，不重复询问。

### 2.2 模型的结构

- 六个生命周期阶段：需求分析、设计、实现、验证、发布、维护。
- 首次准备：仓库初始化、`AGENTS.md` 和工程环境，不在每轮迭代重复执行。
- 跨阶段机制：制品追踪、上下文交接、变更控制、评审、安全、证据和版本管理。
- 原型可用于消除不确定性，但必须标注探索状态；不能自动升级为已确认规格或可发布代码。
- 维护反馈可以返回需求、设计或实现；不要求每次修复重新走完所有阶段。

### 2.3 内容与工具

- 面向人的规范、指南和示例以中文为主；目录名、编号和命令保持稳定。
- 模型暂称“人驱动 AI 的软件生命周期模型”，不为命名研究延迟交付。
- 明确本仓库对 vibe coding 的工作定义，不将其写成行业统一标准。
- 通用提示词不绑定某家 AI 产品；注明所需的文件、终端、浏览器、本地服务和独立会话能力。
- 应用沿用既有架构方向：Next.js、React、TypeScript、CSS Modules、PostgreSQL、Prisma、Vitest、Playwright。执行时核验兼容和安全版本，锁定运行时、依赖和包管理器；主要版本调整须记录 ADR。
- 需要引用外部模型、标准或软件行为时优先使用原始文献和官方文档，记录出处；不把本模型描述为经过标准认证。

## 3. 起始状态与环境约束

截至制定计划时：

- 仓库包含 FocusTask 产品与功能规格，以及开发、初始化、上下文和发布准备指南；还没有应用或 `package.json`。
- 当前有未提交修改：`AGENTS.md`、`README.md`、总指南、DoD、路线图；另有四份未跟踪的流程指南。它们是应保留和迁移的输入。
- 本机可找到 Node.js、npm、Homebrew 和 `psql`；这不证明兼容运行时、PostgreSQL 服务或浏览器测试依赖已可用。
- 当前未发现 Docker 或 Podman 命令；依据用户后续决定，它们不再是实施依赖。

开始实施时重新检查事实：工作区、分支、未提交文件、嵌套规则、端口、运行时、数据库与浏览器。记录到执行状态，不假定上述结果永久有效。

环境准备遵循当前权限。用户已授权的本地准备工作可推进；必要运行时安装或受限网络需要环境授权时提出具体请求。同时继续不依赖该能力的文档与代码工作。使用独立本地进程、端口、数据目录和数据库完成发布验证，不要求虚拟机或容器。

## 4. 目标仓库结构与迁移

```text
README.md                         # 模型定位、阅读与执行入口
AGENTS.md                         # 方法论仓库的实际工作规则
AI_LIFECYCLE_IMPLEMENTATION_PLAN.md
execution/                        # 本计划的进度、决定和阻塞
model/
  overview.md                     # 模型总纲、阶段关系与适用边界
  artifact-catalog.md             # 制品编号、用途、必需性与依赖
  conventions.md                  # 状态、版本、证据和优先级
  tailoring.md                    # 个人、小团队、大团队裁剪
  stages/                         # 六个阶段的操作指南
  artifacts/                      # 每种制品的内容与生成规范
  practices/                      # 初始化、上下文、变更、验证等
templates/                        # 按制品编号命名的可填写模板
examples/focustask/
  README.md
  AGENTS.md                       # 示例应用规则，不覆盖根仓库用途
  docs/                           # 迁移后的原始产品规格与新实证规格
  app/                            # 可运行应用、测试、迁移及部署配置
  evidence/                       # 脱敏实证记录、索引与必要小型证据
  evaluation.md                   # 模型使用效果与局限
scripts/                          # 必要的文档校验工具
docs/                             # 原路径迁移说明，不继续维护双份正文
```

### 迁移规则

1. 先记录现有文件及未提交内容，建立旧路径到新路径的映射。
2. 产品、术语、领域、权限、设计、架构、页面、功能和 ADR 迁入 FocusTask 示例；保留原始产品愿景。
3. 初始化、工作流、上下文、发布准备和 DoD 的通用内容提炼到模型与模板，示例专属约束归入示例。
4. 原 `docs/` 文件及 `AI_PROJECT_DOCUMENTATION_GUIDE.md` 保留简短迁移说明和有效链接；原章节锚点如有引用，提供兼容锚点或更新引用。
5. 新 README 以模型为入口，清楚区分“通用规范”“空白模板”“示例规格”“真实实证”。
6. 根 AGENTS 不再把所有仓库任务限制为 FocusTask 路线图任务。方法论工作使用 `execution/` 任务表；示例应用使用自己的实证路线图。
7. 只迁移与本目标有关的文件；不清理、覆盖或提交无关用户改动，不重写 Git 历史。

## 5. 阶段规范

| 阶段 | 目标与输入 | 主要输出 | 阶段退出条件 |
| --- | --- | --- | --- |
| 需求分析 | 将人的目标、约束和现有资料转换为可执行范围 | 范围、领域与权限规则、功能规格、验收用例、待决策事项 | 关键歧义已处理，验收可观察，未决项不阻塞范围内工作 |
| 设计 | 基于需求消除技术、数据和交互不确定性 | 架构、契约、页面规格、ADR、验证策略 | 关键方案可实现，接口和异常语义一致，风险与验证方式明确 |
| 实现 | 按就绪任务生成和集成实现 | 代码、迁移、测试、评审材料、交接记录 | 自检通过，候选版本明确，证据足够移交独立验证 |
| 验证 | 依据确认标准判断候选版本 | 验收报告、缺陷、回归结果、追踪表 | 适用验收项通过；失败返回对应阶段；无依据不判通过 |
| 发布 | 将已验证候选版本交付到目标环境 | 构建与发布清单、配置、迁移和回退方案、部署与冒烟记录 | 指定产物部署成功，必要数据和核心流程验证通过 |
| 维护 | 将技术问题或新要求转为受控变更 | 问题/变更记录、影响分析、更新制品、修复与再发布证据 | 问题关闭有证据、受影响制品同步、回归和交接完成 |

每份阶段指南包含：触发条件、输入检查、操作步骤、人的决策点、AI 的执行职责、提示词、制品链接、退出检查、失败与返工路径、下一阶段交接指令。

阶段可以重叠开展，但不能跳过前置确认。例如可以探索界面草稿，但不能把草稿中的未确认权限直接用于生产实现。

## 6. 制品体系与最低内容要求

### 6.1 所有制品的共同定义

每种制品必须定义：编号、用途、适用条件、生产者、责任人、使用者、输入依赖、生成与更新时机、输出位置、版本、质量标准、检查方式，以及下一阶段如何使用。

文档另须明确必填章节、字段含义、好坏示例、生成操作指南、空白模板和已填写示例。代码等非文档制品定义目录/格式、生成命令、接口、验证方法和可复现条件。

不是每个任务都生成所有文件。区分“必要”“条件触发”“可合并”；合并文件时仍保留各制品的内容与追踪关系。记录不适用原因，不为填模板制造内容。

### 6.2 制品目录

下表是第一版必须覆盖的制品类型。复合类型可以共享一个文件，但各子项必须有独立章节或可追踪标识。

| 编号 | 制品类型与最低内容 | 下游用途 / 适用性 |
| --- | --- | --- |
| A01 | 范围说明：目标、用户、使用场景、版本边界、非目标、约束、成功条件、来源、未决事项 | 设计与任务拆分；项目必要、迭代增量更新 |
| A02 | 领域与权限：术语、实体字段、关系、状态转换、角色矩阵、时间规则、并发、保留策略 | API、数据模型和测试依据；有业务数据时必要 |
| A03 | 功能规格：任务编号、故事、依赖、前置条件、输入、正常/异常/边界、数据变化、验收用例 | 编码与独立验证；每项业务功能必要 |
| A04 | 架构与契约：组件与边界、技术约束、API 请求/响应/错误、数据约束、依赖、配置、安全和质量目标 | 初始化、编码、集成与发布；必要，深度可裁剪 |
| A05 | UI 规格：布局、内容、交互、状态、响应式、设计令牌、键盘与辅助技术、测试数据 | UI 实现和视觉验收；有 UI 时必要 |
| A06 | ADR：背景、选项、取舍、决定、依据、影响、确认来源、重新评估条件 | 解释重要决定；条件触发，不为每个局部选择创建 |
| A07 | 验证策略：风险、测试层次、用例映射、环境、数据、质量指标、命令、证据要求 | 实现前确定如何验证；必要 |
| A08 | Agent 工作规则：目标、文档入口、范围、决策边界、命令、验证、数据安全、交付格式 | 所有 Agent 的稳定入口；必要，不能复制所有需求 |
| A09 | 工程基线：目录、运行时和依赖锁、配置示例、忽略规则、检查脚本、数据库与最小冒烟测试 | 可复现开发环境；新项目必要 |
| A10 | 任务与依赖计划：任务目标、范围、依赖、就绪条件、负责人、状态、验收与关联制品 | 调度和进度；必要，避免初始化自依赖 |
| A11 | 实现包：源代码、接口、数据库迁移、自动化测试、依赖与变更说明 | 验证和发布；功能变更必要 |
| A12 | 评审记录：候选版本、范围、规格映射、差异、发现、严重度、处置、验证与合并状态 | 集成判断；必要，可为 PR 或本地评审记录 |
| A13 | 会话交接：目标、版本/未提交变更、已读资料、完成项、运行环境、证据、未决项、下一步 | 新会话恢复；切换执行者或长任务中断时必要 |
| A14 | 验收报告：依据及版本、环境、用例、步骤、预期/实际、结论、证据、失败与重测 | 阶段退出；必要 |
| A15 | 缺陷/变更单：触发来源、复现或新要求、影响、严重度、决定、修复/变更计划、回归与关闭依据 | 维护返回需求/设计/实现；条件触发 |
| A16 | 追踪表：需求/AC、设计、代码、测试、证据、评审、版本、当前覆盖与失效状态 | 检查遗漏和变更传播；必要，可自动汇总 |
| A17 | 发布清单：版本、提交/源码快照、制品摘要、配置名称、迁移、检查、已知风险、发布决定 | 确认将要发布什么；每次发布必要 |
| A18 | 部署与回退指南：前置条件、步骤、环境、健康检查、迁移顺序、回退触发、代码/数据兼容、恢复验证 | AI 执行发布与回退；每个部署目标必要 |
| A19 | 发布运行记录：实际时间、产物、命令、结果、部署前后状态、冒烟与持久化证据 | 判断实际交付结果；每次发布必要 |
| A20 | 维护报告：触发、诊断依据、影响、决定、变更、回归、再发布、数据验证和遗留事项 | 维护闭环及后续交接；每次演练必要 |
| A21 | 实际交互记录：提示词来源、参数、实际指令、关键响应、人工决定、输出与验证引用 | 验证人驱动 AI 是否可复现；本次实证必要 |
| A22 | 模型评价：评价问题、采集方法、实际使用、澄清/返工、结果、模型修订和局限 | 判断模型效果；实证前定义问题，实证后填结果 |

## 7. 提示词操作指南的强制要求

### 7.1 每条提示词的组成

每条提示词有稳定编号，并注明使用时机、所需能力、必读材料、参数及如何取得参数、预期输出、完成判断、失败后的下一条指令。

文档制品必须提供“内容规范 + 生成与确认操作指南 + 空白模板 + 填写后的示例”。不能用“请按模板完成”代替澄清、审阅和验证流程。

每个人机交互步骤都有可复制文本；AI 内部的普通文件操作不需要人逐条提示。流程要同时支持已有充分授权的一次执行和需要反馈的分步执行。

### 7.2 必须覆盖的提示词类型

| 类型 | AI 应完成的工作 |
| --- | --- |
| 启动 | 检查上下文、目标、约束和可用能力，复述范围与预期制品 |
| 澄清 | 先读取可发现的事实，再提出影响结果的问题、可选方案和取舍 |
| 方案选择 | 解释影响并记录人的选择，明确哪些工作已经获准 |
| 生成/执行 | 基于确认输入实际生成、修改或运行，返回文件和结果 |
| 审阅 | 从下游用途检查完整性、一致性和可验证性，解释风险 |
| 反馈修订 | 分析反馈影响，更新关联制品并标记需要重验的内容 |
| 独立验证 | 使用确认标准和候选版本执行检查，提供真实证据 |
| 交接恢复 | 固化进度、运行方式、未决项和下一步，让新会话继续 |
| 失败处理 | 在工具失败、信息缺失、验收失败时诊断并回到正确步骤 |

通用类型可共用底层模板，但每种制品必须补充专属输入、检查项和具体示例，不能给所有文档粘贴同一段空泛指令。

### 7.3 先完成一个样板

以 `TASK-001 创建任务` 的功能规格为首个样板，包含正常生成、缺失权限规则、反馈修订、独立检查、交接五条实际操作路径。样板结构检查通过后再批量展开其他制品。

启动提示词示例（完整指南还需提供审阅、修订、验证与交接指令）：

```text
请为 FocusTask 的 TASK-001「创建任务」完善功能规格。
先读取 examples/focustask/docs/ 下的实证范围、领域模型、权限、
既有功能规格和已接受 ADR，并使用 A03 模板。
先报告已知规则与冲突；能从仓库找到的信息不要再次询问我。
对于影响业务范围、权限或数据结果的缺失项，给出选项和影响，
暂停相关定案；已确认部分可以继续形成草稿。
草稿必须包含输入规则、正常/异常/边界流程、数据变化及编号验收用例。
完成后检查规则来源和验收可执行性，返回文件路径、待决策项和检查结果。
不得把你提出的建议写成我已经确认的决定。
```

### 7.4 交互记录真实性

区分“可复制模板”“填写后的示例”“本次实际使用的指令”“人工真实确认”“依据既有授权执行”。历史规格可以迁移作为输入，但不能伪造它们由新提示词首次生成的过程。

执行 Agent 可以展开模板执行已获授权的工作，但要注明是“模板实例化执行”，不能伪装成用户逐条输入。记录关键事实、决定和结果，不要求保存模型隐藏推理，不复制含凭据的聊天记录。

## 8. 状态、权限和证据规则

### 8.1 状态分离

- 任务：待决策、就绪、进行中、待验收、完成、阻塞。
- 文档制品：草稿、待确认、已确认、已替代；确认来源必须可追踪。
- 用例结论：通过、失败、无法判定、阻塞；不适用单列原因，不冒充通过。
- 版本发布决定：go、no-go、无法判定；与用例结论区分。
- 已知发布阻断失败优先判为 no-go；没有已知失败但证据不齐时为无法判定。

候选版本先验收，后集成，再标记任务完成。变更后按影响范围重新验证。不能要求“先已合并才能验收”同时又要求“验收通过才能合并”。发布任务不依赖自身，历史愿景中的未选入功能不阻塞实证版本。

### 8.2 决策责任与自主范围

本计划已确定的本地实证、功能切片、维护演练和常规验证可连续执行。非关键命名、目录细节和兼容补丁选择按现有模式处理并记录。新的业务语义、权限变化、不可逆数据操作或明显扩展范围需由用户决定。

规范中人负责的决定必须配套 AI 分析与记录提示词；不把“由人负责”写成“只能由人独自完成”。模型不要求为每个文档新建一次人工批准；确认可以按范围或迭代批次记录。

### 8.3 独立验证与多 Agent

验证者依据规格、候选版本、环境和证据工作，不把编码者的成功声明作为结论。多 Agent 是可选能力，不是模型使用前提。

实证中的独立验收和新会话接手，需要真实的独立上下文。执行本计划时可为这两个有明确边界的任务使用独立 Agent；验收者只读实现，接手者仅获取仓库入口与交接材料，不继承原始聊天历史。协调者保存任务和结果。

若执行环境无法创建独立上下文，完成其他工作并明确记录该项阻塞；同一会话的第二遍自检不能冒充独立接手实证。

### 8.4 证据完整性

每次关键运行记录：用例/任务、提示词、规格版本、候选提交或源码快照摘要、环境、命令、时间、退出状态、结果与证据位置。未提交源码必须记录差异摘要，不能只引用旧 HEAD。

小型脱敏日志和截图可入库；数据库、依赖、浏览器缓存和构建包不直接入库。大型产物提供本地位置、摘要和重建命令。清理前保留评价需要的记录，不在报告里留下无法定位的证据。

## 9. FocusTask 实证范围与接口

### 9.1 版本范围

保留原产品愿景，新增实证范围及 ADR，解释这是验证模型的切片，不能把原 MVP 全部标为完成。

| 版本/场景 | 必须实现或执行 | 范围边界 |
| --- | --- | --- |
| v0.1.0 | 登录/退出、访问预置项目、成员角色、任务创建与列表、幂等、输入校验、错误反馈 | 项目和用户由隔离测试数据建立，不冒充已实现邀请或创建项目 |
| v0.2.0 | 增加任务状态筛选、URL 状态、空结果、回归、再次发布 | 使用预置的三状态任务测试；不顺带实现任务状态修改流程 |
| 缺陷演练 | 在隔离副本放宽 Viewer 写权限，要求验证者发现、复现、修复并回归 | 明确标注人为注入；不进入正常发布版本 |
| 回退演练 | 发布一个受控失败候选版本，恢复已知可用构建并验证数据 | 不使用删除卷或重建空数据库伪装回退成功 |
| 会话接手 | 新上下文恢复一项进行中的任务，执行下一步并定位证据 | 不提供原始聊天摘要替代仓库交接记录 |

页面包括登录、项目入口、任务列表及创建表单。既有详情、搜索、负责人筛选等不在切片内的操作，在实证页面规格中明确移除或延期，不能保留无法工作的入口。

### 9.2 保留的业务与 API 约束

- Admin/Editor 可创建，Viewer 只读，非成员无法探测项目；服务端每次验证权限。
- 保留创建任务的标题、负责人、日期、幂等与归档项目拒绝规则，覆盖既有 AC-01 至 AC-13。归档和权限变化可由隔离测试夹具触发，不要求提供管理 UI。
- 继续使用 `POST /api/v1/projects/:projectId/tasks`、JSON 协议及 `Idempotency-Key`；列表读接口明确写入契约。
- 登录、退出、项目/成员读取和健康检查接口在设计阶段统一定义，认证使用服务端会话及 HttpOnly Cookie；不以硬编码用户代替登录。
- 实证时区使用夹具中明确记录的 `Asia/Shanghai`，与项目时区规则一致；日期测试采用可控时钟，不依赖固定历史日期。
- v0.2.0 状态筛选参数仅为全部、todo、in_progress、done；非法值的行为在写实现前确认到契约。
- 实证列表使用有限数据集；分页策略保留为后续，不擅自解决原文档中的产品未决事项。

测试身份使用保留示例域名和虚构姓名，密码与会话密钥运行时生成，真实值保存在忽略的本地配置或临时环境中。

### 9.3 工程与部署

- 开发、自动测试、模拟发布分别使用独立配置和数据库，不覆盖其他本地服务。
- 直接运行本地应用和 PostgreSQL，使用专用数据库/数据目录与独立端口；仅绑定本机。启停只针对本任务创建并记录的进程，不干扰已有服务。
- 使用生产构建做发布和回退，不把开发服务器当发布制品。
- 迁移分别验证空库初始化、旧库升级、已经执行过的迁移不被重复应用；不强迫每条迁移 SQL 可任意重复执行。
- 回退报告分别说明代码、配置和数据库的兼容性。本次优先采用向后兼容变更，验证旧应用能读取保留数据；不虚构执行过不可逆迁移回退。
- 配置与部署脚本、测试运行器和证据生成入口应能被另一位使用者复用；真实基础设施凭据不纳入仓库。

## 10. 实证评价设计

在运行实证前定义评价表，实证后填写观察值。默认不做与人工团队的对照实验，不宣称未经测量的成本或效率提升。

| 评价问题 | 采集方法与通过依据 |
| --- | --- |
| 人是否能按指南推动下一步？ | 记录实际提示词和额外澄清；遇到必须靠口头补充的步骤则修订指南后重试 |
| 制品是否指导下游？ | 每个核心制品关联至少一次读取/执行记录及下游产物；未使用的标为未验证或冗余 |
| 文档是否足够明确？ | 记录缺失规则、冲突与返工原因，不以文档篇幅评价质量 |
| 验证是否独立有效？ | 依据确认标准发现注入的 Viewer 权限缺陷；修复后同一用例通过 |
| 需求变化是否正确传播？ | 状态筛选能追踪至范围、规格、API、代码、测试、发布与证据更新 |
| 交接是否可恢复？ | 新会话仅依靠仓库与 A13 完成至少一个真实下一步；记录追加信息请求 |
| 发布与回退是否可信？ | 运行真实生产构建，回退前后数据与核心操作符合预期，有版本和日志 |
| 流程成本是否可观察？ | 统计人工决策次数、澄清次数、失败重试、返工原因和耗时；工具未提供 token/费用则记未采集 |

记录样本限制：单一示例、同一主导执行者、预置故障、工具和环境因素。大型团队裁剪规则标记为设计方案，不能伪装已有企业实证。

## 11. 里程碑与执行顺序

一次推进一个可独立验收任务，完成后更新 `execution/status.md`。下表是执行依赖，不是每一步都要再次向用户索取许可。

| 任务 | 工作与输出 | 验收 / 依赖 |
| --- | --- | --- |
| P0-01 | 现状与环境盘点、迁移映射、执行状态和决策记录 | 原有改动完整保留；识别运行时与本地服务缺口 |
| P1-01 | 模型总纲、阶段关系、状态与责任边界 | 六阶段、准备、跨阶段机制及返工路径清晰 |
| P1-02 | A01–A22 制品目录、统一规范和提示词结构 | 每种制品有下游用途和必需性；依赖无循环 |
| P1-03 | A03 功能规格完整样板 | 含内容规范、模板、填写示例及完整提示词操作链 |
| P2-01 | 全阶段指南、其余制品规范与模板 | 每个人机步骤有提示词，每种文档有专属字段和验证方式 |
| P2-02 | 团队裁剪、迁移与导航 | 模型和示例边界明确，旧入口可用，链接与命名一致 |
| P2-03 | 实证范围、设计、测试策略、评价基线 | 继承的 AC 与范围差异逐项记录；编码前确定验收 |
| P3-01 | 可运行工程、隔离数据库、初始化与检查命令 | 安装、迁移、启动及最小测试实际通过 |
| P3-02 | v0.1.0 完整功能切片 | UI/API/数据验证通过，交付可审阅候选版本 |
| P3-03 | 独立验收、本地评审与首版发布 | 指定版本证据完整，部署与持久化冒烟通过 |
| P4-01 | v0.2.0 状态筛选变更 | 更新制品、回归、再发布形成闭环 |
| P4-02 | 缺陷注入、发现、修复、回归 | 故障与正常版本隔离，前后结果可复现 |
| P4-03 | 失败发布与版本回退 | 恢复已知构建且原数据仍存在 |
| P4-04 | 真实新会话接手 | 用仓库交接完成下一步，有独立运行记录 |
| P5-01 | 制品效果评价和模型修订 | 发现、修订和必要重验关联；报告真实局限 |
| P5-02 | 总体验收和交付 | 本节任务与第 12 节检查全部有实际结论 |

维护演练的第一条操作是生成对应的问题/变更单，不直接跳到改代码。评审记录默认使用本地制品；如果没有托管平台流水线，只能声称本地检查通过，不能标记外部 CI 或 PR 已完成。

## 12. 验证与终态验收

### 12.1 文档与模型

- [ ] 六个阶段、初始化和跨阶段机制都有入口、职责、制品、退出及失败路径。
- [ ] A01–A22 均有规范和示例；文档制品有可填写模板及生成操作指南。
- [ ] 每个人机交互步骤有可复制提示词；占位符、输入位置和后续操作明确。
- [ ] 个人、小团队、大团队裁剪保留必需责任与证据；条件制品说明触发条件。
- [ ] 链接、锚点、模板标识、引用路径和迁移入口有效；示例路径与真实路径可区分。
- [ ] 不存在验收/合并循环、发布任务自依赖或虚构人工确认。

### 12.2 应用与演练

在示例应用目录执行并保存实际结果：

```bash
npm run lint
npm run typecheck
npm test
npm run test:e2e
npm run build
```

- [ ] 测试覆盖合法创建、标题边界、非法负责人、日期、重复提交、重试、权限、归档和数据持久化。
- [ ] UI 覆盖 `1440x900`、`1024x768`、`768x1024`、`390x844`，以及加载、正常、空、失败、字段错误、无权限和适用的只读状态；验证键盘、焦点与缩放。
- [ ] 适用的兼容性、安全与非功能检查具有证据；未执行项明确列出，不拿单一浏览器截图代替全部要求。
- [ ] 首版发布、状态筛选变更、权限缺陷修复、回退和独立会话接手全部真实执行。
- [ ] 版本、配置、迁移、数据库与证据可追踪；回退没有丢弃原数据。

性能指标等原有要求先做适用性映射。需要调整实证范围时必须在运行前记录理由和确认依据，不能在失败后删除要求。

### 12.3 交付报告

最终报告给出：已完成任务与制品、主要路径、实际检查与演练结果、证据索引、模型修订、阻塞与局限。分别报告“模型文档完成”“应用验证完成”“生命周期实证完成”，再给出总体结论。

已知失败、缺少独立接手证据或本地生产构建发布尚未执行时，不得报告整体完成。模板齐全但未使用的制品要明确标为“定义完成、效果未验证”。

## 13. 执行持续性与阻塞处理

执行期间维护以下本地文件，作为长任务及跨会话恢复入口：

- `execution/status.md`：任务状态、完成证据、当前步骤、下一步与未解决事项。
- `execution/decisions.md`：已确认选择及来源、执行者的非关键假设、待人决定的问题。
- `execution/blockers.md`：阻塞动作、命令/错误、影响范围、已尝试方案和解除条件。

遇到问题先检查事实并尝试安全、同范围替代方案；不要为同一个权限错误反复运行无变化的命令。缺少工具时继续不受影响工作，但记录被阻塞的必需实证。

会话结束或切换前更新状态和交接。恢复时读取本计划、执行状态、当前 AGENTS 和最近证据，然后从未完成任务继续；不重新生成已经确认的所有制品。

执行期间不将凭据放入文档、提示词记录、命令输出或 URL。不得引用本次历史聊天中的任何真实密码。故障注入限定在可恢复的测试副本，结束后核对正常版本未受影响。

## 14. 可交给 goal 的启动指令

以下是任务文本，不依赖某种命令行语法；由用户放入 goal 的目标内容。

```text
请完整执行仓库根目录 AI_LIFECYCLE_IMPLEMENTATION_PLAN.md。

目标是建设人驱动 AI 的软件生命周期模型：覆盖需求分析到维护，
详细定义每种制品，并为每个人机交互步骤提供可直接使用的示例提示词。
按计划迁移和复用现有文档，实际实现 FocusTask，直接在本地完成生产构建发布、
需求变更、缺陷修复、版本回退和独立会话接手，用真实证据评价模型。

先读取本计划、当前 AGENTS.md、已有文档和 Git 状态，保留现有未提交改动。
按 P0-01 至 P5-02 的依赖逐项推进，在 execution/ 维护进度、决策和阻塞。
已获授权的常规工作自主连续执行，不为每个步骤重复索取确认；
遇到必须由人决定的新业务规则或受限环境操作时，明确说明依据与影响，
并继续不受影响的工作。发布与维护仅限隔离的本地模拟环境。

每个里程碑提供具体路径和实际验证证据。不得伪造人工确认、测试成功、
外部 CI/PR 状态、独立 Agent 接手或实际部署；示例记录与真实记录必须区分。
无需 Docker；本地服务或独立会话能力不足时应解决前置条件或如实报告阻塞，不能宣称完整实证通过。
不要停在只写规范和模板，也不要为完成示例而扩展成完整产品。

最终逐项检查计划第 12 节，并报告模型、应用、生命周期实证各自的结果及总体结论。
```
