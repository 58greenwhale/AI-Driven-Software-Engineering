# 人驱动 AI 软件生命周期模型 v0.3：方法论修订计划

> 计划版本：1.0 · 编制日期：2026-09-19 · 目标模型版本：0.3
>
> 状态：计划已整理，实施尚未开始。本文供后续 goal 执行；保存本文不代表启动执行，也不代表模型通过验收。
>
> 依据：用户关于演化型原型、四项开发活动、同类问题排查、多来源设计和活动裁剪的确认。本轮只落实方法论及配套文档，新增应用实证暂缓。

## 1. 目标、范围与执行依据

### 1.1 本轮目标

将现有模型升级为以**演化型原型**为核心、由人驱动 AI 完成的软件生命周期模型。开发过程由四项活动组成：

**原型构建 → 原型重构 → 原型审查 → 原型精修**

这一顺序是默认组织方式，不是项目级固定流水线。活动可以按功能或迭代开展，可以裁剪、合并、交叠，也可以在依赖允许时对不同功能并行开展。

整体生命周期表达为：

```text
目标与最小准备
  → 按项目配置组织开发活动
      原型构建 ⇄ 原型重构 ⇄ 原型审查 ⇄ 原型精修
  → 独立验收 → 发布 → 维护与下一轮演化

体验反馈融入原型构建；测试、文档同步和同类问题排查贯穿全过程。
发现问题按影响返回相关活动，不要求每次重走全部流程。
```

需求分析、设计和实现仍然存在，但融入开发活动，不再要求它们先作为三个封闭阶段顺序完成。独立验收、发布和维护继续保留。

### 1.2 本轮交付与非目标

本轮交付包括模型总纲、四项活动指南、裁剪与协作规则、制品规范、空白模板、教学示例、操作提示词、图解、文档校验工具和静态验收记录。

本轮不开展以下工作：

- 新的 FocusTask 功能开发、应用重构、业务规格变更或 UI 改版。
- 启动应用或数据库、执行新的发布、维护、回退及故障演练。
- 安装 Docker，建设真实生产运维体系，扩展市场发现、商业可行性或增长运营方法。
- 创建提示词平台、模型执行平台或新的产品项目。
- 自动提交、推送、远端 PR、联系外包人员或购买设计服务。

模型仍说明如何由人驱动 AI 完成本地发布与维护；本轮只更新这些指南的衔接。视觉设计的不同来源只提供方法与教学示例，不要求本轮实际调用图像服务或获取外包设计。

### 1.3 已核对的起点

编制时，[当前模型](model/overview.md)为 v0.2；[原实施计划](AI_LIFECYCLE_IMPLEMENTATION_PLAN.md)对应任务及后续图解任务在[执行状态](execution/status.md)中均已完成。它们是历史输入，不是本轮待重新执行的任务。

当前已有 A01–A22、六份阶段指南及三组 Mermaid/SVG/PNG 图解。`scripts/check-docs.mjs` 写死了 22 类制品和六阶段；图解渲染脚本写死了三张图，均需随新模型调整。

编制时实际运行 `node scripts/check-docs.mjs`：172 份 Markdown、538 个本地链接、22 类制品、6 个阶段，错误为 0。此结果仅是修改前检查，不是 v0.3 验收证据。

仓库存在大量未提交和未跟踪内容。执行前重新检查事实，保留现有工作，不执行清空、回退或覆盖无关文件的操作。

### 1.4 计划优先级与完成定义

执行前阅读根 [AGENTS.md](AGENTS.md)、本计划、执行状态、模型总纲及[约定](model/conventions.md)。旧计划用于理解背景和历史结果；本轮任务、范围与完成标准以本计划和后续明确的用户决定为准。

最终读者应能仅凭仓库材料回答：如何组织活动、需要哪些输入、如何驱动 AI、输出什么、怎样检查、如何交接以及出错后如何返回。

全部必需交付和静态验收完成后，报告：**v0.3 方法论与文档静态验收完成，新增应用实证未开展。** 不以文件数量、旧应用测试结果或执行者声明替代验收。

## 2. 已确认的方法论与默认规则

### 2.1 人驱动 AI 与演化型原型

人负责目标、关键业务规则、重要取舍及风险接受；AI 协助分析、提出方案、生成制品、执行和验证。每个需要人驱动 AI 的环节都提供示例提示词，不要求人独自撰写任何工程文档。

AI 在已有授权内连续工作，常规命名、排版、检查和范围内修复不重复询问。未确定的关键业务规则不能伪装成用户确认；原型中可显式使用假设或模拟来帮助讨论，不能据此执行未经授权的真实业务行为。

原型是持续演化的软件及其需求认知载体。下一版本以当前原型、反馈和已确认结果作为输入。即使 AI 替换全部代码，也属于原型演化；不以代码保留比例判断原型是否被抛弃。

必须追踪的是目标、行为、约束、决定和版本变化。临时实现、模拟数据、缺陷或未经确认的交互不会自动成为正式需求；已确认需求也可以经受控变更调整。

### 2.2 原型构建

原型构建同时完成需求获取、需求分析、设计、实现、体验反馈与需求澄清，形成一个内部循环：

```text
顶层目标与当前功能细节
  → 人驱动 AI 分析需求，形成或更新功能 SOP
  → 人驱动 AI 设计方案，形成或更新设计与实施 plan
  → 人驱动 AI 生成代码、测试及可运行原型
  → 人体验并反馈，AI 整理差异、问题与待决事项
  → 同步更新 SOP、plan 和原型，进入下一轮
```

- 输入：顶层目标、当前选定功能、已有原型、约束和已知反馈。
- 输出：A03 功能 SOP、A10 设计与实施计划、A11 实现包、A23 原型版本与反馈确认记录，以及受影响的范围和领域规则。
- 出口：本轮选定行为得到确认，运行入口可用，模拟边界与未决事项明确，足以进入下一项适用活动。
- 边界：不要求提前细化整个产品，也不要求原型构建完成全部目标架构；适用的数据保护、权限和安全底线从构建开始落实。

SOP 是可执行和验收的功能规格，包含流程、规则和异常语义，不仅是页面操作说明。plan 同时包含设计依据和实施安排，不仅是任务清单。

### 2.3 原型重构

在选定原型行为得到确认后，根据技术栈、架构和非功能要求演化实现。允许业务逻辑拆分、模块调整、契约完善、模拟实现替换、性能与安全改造，以及必要的整体重写。

- 输入：当前原型、确认的 SOP、现状和目标技术约束、适用质量要求。
- 输出：A24 工程化差距与重构方案、更新的代码和测试、A04 架构与契约、必要的 A06 ADR，以及 A08 对应的 AGENTS.md。
- 出口：差距按计划处理，确认行为得到保持或正式变更，适用检查通过，遗留风险及处理方式明确。
- 边界：不能只修改 AGENTS.md 就宣称重构完成；技术规则、实现和验证必须一致。新的业务取舍返回需求澄清，不由重构默默改变。

AGENTS.md 写长期有效的工作约束及入口，具体架构、契约和业务规则保存在对应制品。当前活动、任务进度和临时结论通过计划及状态入口获取，避免持续堆入 AGENTS.md。

### 2.4 原型审查

重点是审查实现，并让 AI 生成便于人理解、检查和追问的内容，作为项目持续维护的文档。

- 首次系统审查逐行覆盖范围内全部自有实现，包括业务代码、测试、配置、迁移和脚本；可按模块分批完成。
- 后续审查覆盖差异及受影响路径；大范围重写重新建立覆盖范围，不能沿用已失效的逐行审查结论。
- A12 记录候选版本、文件及代码区段、审查问题、未覆盖原因和复查结果。依赖、生成文件和二进制资产单列检查方式。
- A25 提供模块关系、数据流、关键路径、SOP 到实现的映射、设计理由、风险与需人决定的问题，链接代码位置及版本。
- A26 记录每种问题模式的全项目排查与修复。全部覆盖不等于证明不存在缺陷，代码审查不替代独立验收。

人可根据实现解读开展分层审阅并下钻到具体代码；不要求人手工编写说明，也不以机械逐行复述代替解释。代码变化后同步更新受影响的解读和覆盖记录。

### 2.5 原型精修

功能符合要求后，获取更合适的视觉与交互设计，再由 AI 按设计调整页面和相关实现。默认位于原型审查之后；设计探索可提前，最终改动仍需重新审查及回归。

设计支持三种来源：专门的设计 AI、人工设计或外包、两者混合。

- 设计 AI：按实际工具能力和表达习惯组织提示词，提供文生图与图生图的教学示例。通用要求明确目标、参考、约束与输出，不固定一家产品的专有语法。
- 人工或外包：使用设计简报、交付清单和反馈记录；AI 协助整理需求、解释差异、比较方案及检查交付完整性。
- 混合设计：记录各部分来源、版本、修改关系与使用限制，不把人工设计伪写成 AI 生成。

统一设计交付包括目标、参考、效果图与源文件、交互说明、加载/空/错误等状态、响应式要求、可访问性、素材来源、行为差异以及确认记录。静态效果图无法表达的交互必须补充说明。

A27 保存设计交付及确认过程；A05 保存当前用于编码和验证的 UI 与交互规格。AI 依据确认后的交付包实施，不把未确认图稿自动视为新业务要求。

精修可引起局部功能调整，但整体目标不变。依据权限、数据、业务流程及验收标准评估影响，不按修改行数判断：仅表现变化更新设计与视觉检查；行为变化还需更新 SOP、plan 和受影响验收项；整体目标变化返回范围决策。

### 2.6 同类问题排查是横贯规则

“问题批量改”指从一个问题举一反三，排查整个项目的同类问题，不是单纯把若干无关问题集中修改。

```text
复现问题 → 提炼根因与模式 → 全项目搜索相同问题及变体
  → 区分已确认、疑似和不适用 → 修复确认项
  → 回归验证 → 再次排查 → 沉淀规则并更新相关制品
```

排查同时使用文本、结构及业务语义，不能只搜索相同代码片段。疑似项先验证，不能按相似度直接替换。未发现其他实例也需说明搜索范围、方法、结果和限制；未检查部分不能写成没有问题。

此规则应用于四项活动、验收发现的缺陷和维护。即使本次任务或代码审查仅覆盖局部，全项目同类问题排查仍须尝试；不能检查的模块明确记录。

配套提示词至少达到以下具体程度：

```text
已发现问题：{问题、复现、候选版本及位置}。
请先解释根因，提炼可能重复出现的代码模式和业务条件，再检查整个项目。
不要局限于相同文本；检查同类调用、相似模块、替代写法和相关异常路径。
将实例分为已确认、疑似、不适用，并说明依据、覆盖范围和未能检查的部分。
在已有授权内修复确认实例，补充有针对性的回归验证，修复后再次排查。
把结果写入 A26，关联 A12/A15/A16；必要时更新实现解读和稳定工作规则。
缺少会改变业务语义的决定时暂停相关修改，其余已确定工作继续。
未执行的检查不得报告通过，未发现其他实例也要保存实际排查依据。
```

### 2.7 裁剪、组合与并行

| 组织方式 | 执行方式 | 必须解释的组织条件 |
| --- | --- | --- |
| 按功能 | 每项功能依次执行适用活动，不同功能可并行 | 功能边界、共享接口、依赖、工作区或文件归属、集成顺序 |
| 按迭代 | 本轮功能整体构建，再整体重构、审查、精修 | 本轮范围、统一约束、候选版本、整体交接及验收 |
| 混合组织 | 先统一公共约束，各功能分别推进，最后集成 | 公共制品责任、变更传播、冲突处理、集成后的重验范围 |

A10 记录执行粒度、采用的活动、合并/简化/跳过理由、依赖、并行边界、共享制品和集成条件。不同功能可处于不同活动，不能让一项功能的就绪状态代表全部功能。

无 UI 项目可省略视觉精修；已满足工程约束的原型可简化重构；小改动可合并活动。裁剪活动形式不取消适用的确认、测试、审查及证据要求。

并行时共享 SOP、领域权限、接口、架构和设计规则有明确维护入口；冲突必须解决并更新受影响功能，集成后验证跨功能路径。方法论支持并行，不表示本轮执行必须采用多个 Agent。

### 2.8 初始化与独立验收边界

首次准备重点讲清 AGENTS.md 应写什么，其他工程基线提供清单和模板，避免把完整基础设施建设设为构建原型的前提。AGENTS.md 最低内容为：

1. 项目目的、范围、非目标与目录边界。
2. 可信资料、必读入口、规则冲突处理方式及子目录规则范围。
3. 技术栈、架构边界、命名和编码规范；详细规范用链接引用。
4. 当前计划、状态和任务入口；演化及同类问题排查规则。
5. 已授权执行范围、需要人决定的事项与交付要求。
6. 实际存在的开发、检查、测试和构建命令及工作目录。
7. 验证、候选版本、证据、数据安全和敏感信息要求。

不存在的命令只能列为待建立，不能写成已可用。工程基线清单覆盖目录、依赖锁、配置示例、忽略规则、检查入口和最小运行验证，按项目需要逐步增强。

独立验收依据确认规格和固定候选开展，验收者不修改实现；发现问题交回修复，再对新候选复验。独立性体现为职责、依据与执行记录，不强制购买另一款 AI，也不把实现者自检称为独立验收。

## 3. 制品、提示词与文档接口

### 3.1 更新 A01–A22

保留已有编号、文件路径和制品索引字段，只调整标题与内容。每类制品同步检查规范、模板、教学示例及下游引用。

| 制品 | 修订重点 |
| --- | --- |
| A01–A03 | 区分顶层目标与逐步澄清的细节；A03 标题明确为功能规格（SOP），包含正常/异常/边界、权限、数据变化和验收要求 |
| A04–A06 | 区分当前实现、目标约束与确认设计；记录重构、技术选型和精修中的重要决定 |
| A07 | 区分体验检查、行为保持、工程质量、审查后回归、视觉交互验证和独立验收 |
| A08–A09 | 补齐 AGENTS.md 的内容与生成指导；基线按最小可运行准备逐步增强 |
| A10 | 标题改为设计与实施计划（plan）；包含方案与理由、任务、活动裁剪、并行边界、依赖、验证和集成条件 |
| A11–A14 | 增加原型演化来源、覆盖记录、实现解读入口、交接和独立验收边界 |
| A15–A16 | 串联反馈、确认、规格、代码、审查和证据失效；关联同类问题实例 |
| A17–A20 | 保留直接本地发布与维护，更新开发活动衔接、缺陷返回及同类排查路径 |
| A21–A22 | 区分真实交互与教学提示词，记录活动裁剪效果和未实证的假设 |

不要求每个项目生成全部文件。按用途区分必需、条件触发和可合并；轻量项目用少量文件承载相关内容，不能为了填齐目录虚构制品。

### 3.2 新增 A23–A27

下表基名分别用于 `model/artifacts/`、`templates/` 和 `examples/focustask/docs/artifact-examples/`，均使用 `.md` 后缀。三类文件分别是规范、空白模板和已填写教学示例。

| 编号与基名 | 制品 | 必填内容 |
| --- | --- | --- |
| A23-prototype-feedback | 原型版本与反馈确认记录 | 原型版本、运行入口、当前范围、真实与模拟边界、假设、反馈、确认来源、未决项及版本演化关系 |
| A24-refactoring-plan | 工程化差距与重构方案 | 当前/目标状态、差距、保留/调整/替换策略、步骤与依赖、行为映射、检查方式、失败恢复 |
| A25-implementation-guide | 面向人的实现解读 | 模块关系、关键流程、SOP 与代码映射、重要判断、设计理由、风险、人需决定的问题及依据的代码版本 |
| A26-pattern-remediation | 同类问题排查与修复记录 | 根因及模式、全项目范围与方法、确认/疑似/不适用实例、修复、回归、再次排查、剩余风险、规则更新 |
| A27-design-delivery | 视觉与交互设计交付包 | 设计简报、来源及工具、参考与效果图、源文件、交互和状态、响应式、素材来源、行为变化、反馈与确认 |

A12 管理审查过程和结论，A25 保存持续维护的实现说明，A26 管理具体问题模式的排查闭环。A24 聚焦重构差距，实施任务统一关联 A10；A27 管理设计交付，当前 UI 行为以 A05 为实现入口。

允许合并文件，但必须保留清晰章节、标识和关系，避免复制出多份相互冲突的规则。

每类规范定义用途、适用条件、输入及版本、生产者与使用者、生成和更新时机、必填内容及字段含义、好坏示例、检查标准与下游用途。新增文档沿用既有必要章节，包括“必填内容与字段含义”和“质量标准与边界”。

### 3.3 提示词与教学案例

每类制品保留九种提示词标识：`START`、`CLARIFY`、`SELECT`、`GENERATE`、`REVIEW`、`REVISE`、`VERIFY`、`HANDOFF`、`FAILURE`。标识使用现有 `[Axx-类型]` 形式。

每项开发活动和后续阶段包含七种操作提示词：`START`、`DECIDE`、`GENERATE`、`REVIEW`、`VERIFY`、`HANDOFF`、`RECOVER`。内容必须符合对应活动，不能只替换名称复制同一段。

每条提示词注明使用时机、参数及来源、所需 AI 能力、任务与决策边界、输出要求、完成判断、下一步及失败处理。工具尚未选定时先确认所需能力；不编造专有参数或工具运行结果。

必须覆盖以下可复制示例：

1. 从顶层目标和部分功能细节启动原型。
2. 根据体验反馈同步更新 SOP、plan 和代码。
3. 按技术与架构要求重构，并更新稳定工作规则。
4. 建立逐行审查覆盖清单，生成人可审阅的实现解读。
5. 根据一个问题排查全项目同类实例、修复并复查。
6. 驱动设计 AI 生成效果图或基于参考图修改设计。
7. 准备人工设计简报，检查交付并整理反馈。
8. 按确认设计调整页面，分析功能影响并回归。
9. 选择活动裁剪方案，安排并行功能及集成验证。
10. 向独立验收交接固定候选，处理失败和重新验收。

使用“FocusTask 创建任务及任务列表体验”这一虚构教学演化场景串联四项活动，不新增真实产品承诺。教学参数依据现有规格选取，不把示例假设回写进已确认业务规则。

教学文件统一包含“教学填写示例”“不作通过结论”，说明未实际执行。无需为了示例创建应用、生成真实效果图、联系设计师或伪造确认。引用旧证据时明确其仅支持原版本，不能证明新版示例已经运行。

### 3.4 索引、兼容与校验接口

`model/artifact-index.json` 继续使用现有数组及 `id/title/spec/template/example` 字段，新增五条，保留 A01–A22 的路径。

新增 `model/workflow-index.json` 作为活动和后续阶段的检查入口。采用条目数组，最少包含 `id`、`title`、`kind`、`guide`、`promptPrefix`：`kind` 区分 `activity` 与 `stage`，`guide` 使用仓库相对路径。收录四项活动和独立验收、发布、维护三个阶段；初始化仍作为跨阶段实践，不凑成第五项开发活动。

原有 `node scripts/check-docs.mjs` 命令保持可用。检查器改为读取索引，保留原有 Markdown、链接、章节、提示词和教学边界检查；缺失文件输出明确错误，不能以未捕获异常结束后丢失其余诊断。

为隔离测试增加可选 `--root <目录>` 参数，默认仍检查仓库根目录。测试使用 Node.js 自带测试能力和临时夹具，不新增应用依赖。结果分别统计 `activities` 和 `stages`，不把七份指南统称七阶段。

本轮清单验收另外核对 A01–A27 以及“四活动＋三阶段”是否齐全，不能因为索引漏项而让检查器默认为无须检查。无需建设通用索引平台或复杂 schema 系统。

## 4. 分步执行与文件迁移

### 4.1 任务顺序与交付条件

下列任务是本轮唯一待执行序列。除记录明确阻塞外，按依赖顺序完成，每次推进一个可独立检查的任务。大任务可以分小批编辑，但不得因批次完成提前宣布整个任务完成。

| 任务 | 依赖 | 工作与交付 | 任务完成条件 |
| --- | --- | --- | --- |
| V03-01 基线与恢复入口 | 无 | 盘点规则和工作区；建立基线、修改边界、迁移表与执行记录 | 原有工作可区分，受保护文件有摘要，恢复入口可用 |
| V03-02 总纲与裁剪 | V03-01 | 修订模型定位、原则、生命周期、裁剪、初始化和跨阶段实践 | 四项活动及三种组织方式一致；输入、决策和返回关系清晰 |
| V03-03 活动指南 | V03-02 | 新增四份活动指南；调整独立验收、发布、维护的衔接 | 每份操作链完整，提示词可用，旧指南已有明确迁移去向 |
| V03-04 制品与提示词 | V03-03 | 更新 A01–A22；新增 A23–A27 的三类文档；串联教学案例 | 内容、模板、示例、提示词和下游用途一致 |
| V03-05 索引与兼容切换 | V03-04 | 更新两类索引、文档检查器及测试；将旧前三阶段指南改为兼容入口 | 新索引完整、校验正反例有效、旧路径和锚点可用 |
| V03-06 导航与图解 | V03-05 | 更新 README、根 AGENTS、总指南入口和四组图解及渲染脚本 | 当前入口统一指向 v0.3；图文一致，实际导出可读 |
| V03-07 场景验收与交付 | V03-06 | 固定候选，检查、走查、保护校验，修复后复查并交付 | 第 5 节必需项通过，证据完整，局限明确 |

迁移期间尚未生成的目标用代码形式记录规划路径，不创建断链。V03-03 先保留旧指南及其提示词；V03-04 先完成新增文档。到 V03-05 时将新索引、检查器和旧指南兼容入口一起切换，避免旧校验器仍要求已移除的六阶段结构。

每项完成时执行适用的结构检查和语义检查。中途已知待切换项要记录，不为清零错误临时删除检查规则；最终检查必须完整通过。

### 4.2 修改区域与保护区域

可修改区域：`model/`、`templates/`、文档与图解相关 `scripts/`、根 README/AGENTS/总指南、必要的 `docs/` 兼容入口，以及 FocusTask 的教学示例目录。FocusTask README 仅可补充教学与历史实证的版本边界和导航。

受保护区域：现有历史计划、已完成任务及验收/交付报告、既有基线、FocusTask 实际应用、真实证据、实证评价、应用 AGENTS 和已确认业务规格。历史材料中的原阶段名称和原编号范围保留，不全仓替换。

根 `execution/status.md` 只增补本轮入口和状态；保留原任务和恢复事实。根 AGENTS 在 V03-06 增补当前计划及新活动规则，同时保留已有安全、证据和工作区保护要求。

### 4.3 基线与执行记录

V03-01 创建 `execution/v03/`，最少维护：

- `status.md`：任务状态、当前步骤、检查结果、阻塞和下次恢复位置。
- `inventory.md`：原有改动、允许修改与保护的文件集合、摘要及工具事实。
- `migration.md`：旧入口到新入口、制品和锚点的映射。
- `decisions.md`：已确认决定和本轮必要的实现取舍，区分用户决定与执行者选择。
- `validation.md`：验收清单、候选摘要、实际命令、走查结果、失败及复查证据。
- `delivery.md`：范围、变更文件、结果、限制和下一步。

首次执行时，将拟修改的既有文档、模板、脚本和图解复制到 `execution/v03/baseline/`，保留相对路径及摘要清单。后续扩大修改集合时，在第一次编辑前补充原始副本，绝不覆盖已保存基线。

受保护的自有源文件和历史证据记录文件清单及摘要，不复制依赖缓存、构建缓存、本地数据库或敏感配置。最终保护检查核对新增、删除和内容变化；说明排除范围。现有 `execution/baseline/` 原样保留。

基线只作为档案，不作为当前 Agent 指令。新增指南或制品在状态中注明“新建”，不要把它们混入修改前快照。执行过程的新检查记录放在本轮目录，不覆盖旧报告。

### 4.4 指南与导航迁移

新增以下活动指南：

| 路径 | 内容 |
| --- | --- |
| `model/stages/prototype-building.md` | 原型构建及反馈澄清循环 |
| `model/stages/prototype-refactoring.md` | 工程化差距、实现演化及规则同步 |
| `model/stages/prototype-review.md` | 审查覆盖、实现解读及同类问题闭环 |
| `model/stages/prototype-polishing.md` | 多来源设计、页面改造及影响验证 |

保留 `verification.md`、`release.md`、`maintenance.md`，更新输入与返回路径。原 `requirements.md`、`design.md`、`implementation.md` 最终保留迁移说明和对应入口，不维护第二套正式流程。

旧锚点被引用时保留兼容锚点或同步更新活动引用；真实历史文档不强行重写。README、总纲、图解说明、制品目录和总指南兼容页明确区分当前方法论、空白模板、教学示例与历史实证。

### 4.5 图解交付

| 图解基名 | 必须表达的关系 |
| --- | --- |
| lifecycle | 目标准备、可组合开发活动、独立验收、发布、维护及反馈返回 |
| collaboration | 构建内反馈、人可审阅的实现解读、同类排查、多来源设计及确认 |
| artifacts | A01–A27 在活动间的主要输入输出与追踪关系 |
| organization | 按功能并行、按迭代顺序、混合组织三种方式及集成点 |

每张图交付 Mermaid 源文件、SVG 和 PNG，位于 `model/diagrams/`。Mermaid 是唯一可编辑源，导出图由渲染脚本生成；图解说明补充完整图例与裁剪含义，避免把默认顺序画成不可变流程。

复用 `scripts/render-model-diagrams.mjs` 及已有配置，增加第四张图，保留现有 CLI 参数。执行时重新确认渲染器和浏览器是否可用，记录真实版本；必要工具使用任务隔离的临时目录，不依赖过去记录的临时绝对路径，不引入 Docker 或应用依赖。

## 5. 验收、恢复与完成标准

### 5.1 验收清单

| 编号 | 验收项 | 通过依据 |
| --- | --- | --- |
| AC-V03-01 | 模型原则完整且一致 | 四项活动、演化定义、人驱动 AI、反馈位置和独立验收边界均可定位；无相互冲突的活动正文 |
| AC-V03-02 | 裁剪可执行 | 三种组织方式及无 UI、小改动情形都有输入、理由、依赖和集成条件 |
| AC-V03-03 | 制品完整且衔接明确 | A01–A27 规范/模板/教学示例齐全，既有路径稳定，新增制品用途无含混重复 |
| AC-V03-04 | 提示词可使用 | 九类制品提示词、七类活动/阶段提示词及第 3.3 节场景完整，参数与交接清晰 |
| AC-V03-05 | 文档检查通过 | 运行文档检查器，退出码为 0；包含本轮新增内容，不通过扩大忽略范围隐藏问题 |
| AC-V03-06 | 检查器行为可信 | 正反例按预期通过或报告失败，覆盖缺失文件、重复标识、缺失提示词及教学边界 |
| AC-V03-07 | 图解可复现且可读 | 四张图实际导出 SVG/PNG，文件有效，逐张视觉检查及语义核对通过 |
| AC-V03-08 | 场景走查完整 | 第 5.3 节全部场景能从具体指南、制品和提示词找到执行路径 |
| AC-V03-09 | 历史保护有效 | 受保护清单及摘要比较通过；旧计划和实证未被改写成新模型证据 |
| AC-V03-10 | 恢复与交付明确 | 状态、迁移、决定、验证和交付记录完整，明确实证未开展及剩余验证假设 |

每项结果使用“通过、失败、无法判定、阻塞”，并给出版本和证据。必要验收项不能改为不适用以绕过失败；项目裁剪示例中的不适用事项另列理由。

### 5.2 必须执行的检查

文档和检查器测试：

```sh
node scripts/check-docs.mjs
node --test scripts/check-docs.test.mjs
```

第二条是本轮待建立的测试入口，当前不能视为已有或通过。测试使用临时夹具，至少覆盖正常输入、缺失目标、重复制品/流程 ID、缺失必要章节或提示词、缺失教学边界、活动/阶段分类以及既有断链检查；不能通过修改真实文档制造测试失败。

渲染工具在 PATH 可用时：

```sh
node scripts/render-model-diagrams.mjs
```

否则使用脚本已有 `--cli`、`--puppeteer-config` 参数指定实际可用路径，并记录完整命令。实际查看四张图的 SVG/PNG，检查文字、截断、连线、图例、节点遮挡及默认顺序的表达。

另外完成以下语义检查：

- 对照迁移表检查旧入口和锚点，以及规范、模板、示例和索引的一致性。
- 搜索活动正文中仍把旧六阶段当当前主流程的表述，逐项处理；排除明确的历史说明，不要求删除合法历史用语。
- 检查是否错误地把所有细节和工程基线完成设为原型构建前提，或把安全底线统一推迟到重构。
- 检查是否仍把“批量改”只解释为集中修复，是否遗漏全项目语义及变体排查。
- 检查设计来源、真实确认及教学数据的边界，避免默认所有视觉制品必须由编码 AI 生成。
- 核对受保护文件集合与摘要，报告任何新增、删除或变化。

本轮不修改应用，故不要求重跑 FocusTask 的 lint、单元、E2E、构建或发布命令。旧通过结果保持历史含义，不能填入本轮新验收项。

### 5.3 静态场景走查

| 场景 | 必须可从文档找到的处理路径 |
| --- | --- |
| 只有顶层目标及部分细节 | 能启动构建；确认必要规则，显式标记假设、模拟与待决项 |
| 体验反馈改变功能细节 | 在构建活动内同步更新 SOP、plan、代码和确认记录 |
| 原型实现被整体替换 | 仍按演化处理；行为、版本与决定可追踪，旧覆盖和证据按影响重验 |
| 一个缺陷存在多个变体 | 提炼根因，全项目排查，区分确认/疑似/不适用，修复并复查 |
| 第一轮审查与后续局部变更 | 首轮自有实现覆盖完整；后续检查差异和影响，同类问题仍查全项目 |
| 使用设计 AI 或人工设计 | 各自有输入及协作方式，最终满足同一设计交付要求 |
| 精修改变局部行为 | 能识别表现变化、业务变化和范围变化，更新相应制品与验证 |
| 多功能并行或按迭代推进 | 共享约束、依赖、工作边界、冲突处理及集成验证明确 |
| 无 UI 或极小改动 | 能裁剪或合并活动，适用质量责任仍存在 |
| 独立验收失败或维护反馈 | 返回受影响活动，关联同类排查，固定新候选后重验 |
| 历史实例缺少新增制品 | 标注历史版本与局限，不补造 A23–A27 的真实使用记录 |
| 新会话接手 | 根据计划和状态找到当前任务、输入版本、未决项和下一步 |

每项走查记录引用的具体指南、制品和提示词，以及发现、修改和复查结果。这些是文档可执行性检查，不是新的应用实证或真实人机使用实验。

### 5.4 执行状态与恢复

执行状态区分待执行、进行中、待检查、完成和阻塞。每次完成任务、遇到阻塞或结束会话时，记录当前子步骤、修改范围、实际检查、结果、未决事项及下一项可执行动作。

工具不可用时记录受影响检查并继续无依赖工作；未执行的检查不能写成通过。需要改变业务或模型边界的新决定时，只暂停依赖该决定的工作；范围内常规选择按本计划执行并记录。

新会话先读取根规则、本计划、根执行状态及本轮状态，再检查工作区与候选摘要，从未完成步骤恢复。不要因上下文丢失重做基线或覆写原文件。

最终检查绑定具体候选文件及 SHA-256 摘要。待验收内容固定后再检查；若修复使候选变化，更新候选并重验受影响项。验收报告和交付记录作为检查后的记录单独列出，避免要求它们包含自身摘要。

本轮文档自检如由执行者完成，明确标为自检；未实际开展独立复核不得声称完成了独立验收。后续如另行开展独立复核，复核者只读确认标准和固定候选，修复由执行者处理。

### 5.5 交付与剩余假设

最终报告列明完成范围、变更文件、命令及结果、图解与走查证据、历史保护结果和未完成事项。所有必需验收项通过，才将 V03-07 及本轮目标标为完成。

需要如实保留的局限：四项活动的成本收益、全量代码重写后的行为保持、设计交付的实际协作效率、跨功能并行组织效果，尚需新的应用实证验证。本轮不为了填补这些证据启动额外开发任务。

后续应用实证须另立范围、候选和验收标准；不能继承本轮文档通过结论作为应用结果，也不能继承 v0.2 的测试结果证明 v0.3 方法有效。

## 6. 交给 goal 的执行文本

以下是目标描述，可在准备启动 goal 时使用；本文本不在计划落盘时自动执行。

```text
依据 AI_LIFECYCLE_V03_REFINEMENT_PLAN.md，完成软件生命周期模型
v0.3 的方法论修订、制品规范、模板、提示词、教学示例、图解和文档校验。

先读取根 AGENTS.md、根执行状态、本计划及相关模型文件。
旧 AI_LIFECYCLE_IMPLEMENTATION_PLAN.md 用于理解历史，不重新执行其中
已完成的应用开发与实证任务。本轮按 V03-01 至 V03-07 顺序推进。
保护当前未提交改动和历史实证，每次完成一个可独立检查的任务，
更新 execution/v03/ 的状态、决定、阻塞和证据，保持根恢复入口有效。

落实原型构建、原型重构、原型审查、原型精修四项可组合活动；
明确演化型原型、反馈融入构建、面向人的实现解读、
全项目同类问题排查、多来源视觉交互设计及活动裁剪。
保留 A01–A22 的编号与路径，新增 A23–A27，
完成相关规范、模板、教学示例和人驱动 AI 的操作提示词。

本轮不开展新的 FocusTask 应用开发、部署或故障演练，
不使用 Docker，不修改真实历史证据，不提交或推送。
教学示例和静态走查不得冒充实际运行、设计交付或人工确认。
不要为了证明模型有效擅自启动后续应用实证。

完成文档校验、校验器针对性测试、四张图的实际导出与检查、
场景走查及历史保护检查。修复发现并重验受影响内容。
沿用已确认决定，在授权范围内持续执行；遇到必须由人决定的事项，
记录具体阻塞并继续不依赖该决定的工作，不猜测确认或降低验收标准。

只有所有必需交付和静态验收完成，才能报告目标完成。
最终提供交付及验收记录，明确说明：
“v0.3 方法论与文档静态验收完成，新增应用实证未开展。”
若仍有必需检查失败、无法判定或阻塞，如实保留未完成状态。
```
