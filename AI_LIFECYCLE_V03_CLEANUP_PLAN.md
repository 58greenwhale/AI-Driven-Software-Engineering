# 最新生命周期方案：仓库清理执行计划

> 版本：1.0 · 日期：2026-09-19 · 状态：清理完成，勿重复执行
>
> 用途：清理任务依据。实际提交、删除和检查见[清理记录](execution/cleanup.md)，不得根据计划正文推断完成状态。
>
> 已确认：只保留最新方案相关内容；FocusTask 只保留教学材料；先提交一次，再直接删除旧内容，不移出仓库、不另建备份。

## 1. 目标与执行边界

将仓库整理为“最新生命周期方案、可复用规范与模板、纯教学示例”。最新方法论见 [v0.3 修订计划](AI_LIFECYCLE_V03_REFINEMENT_PLAN.md)：原型构建、原型重构、原型审查、原型精修四项可组合活动。

本计划只完成清理及其必要的引用、校验和入口修复，不执行完整 v0.3 建设。清理后，新活动指南、A23–A27、新版图解及新增应用实证均不能标为已完成。

执行前读取根 [AGENTS.md](AGENTS.md)、本计划、修订计划和当前执行状态。关于“保留旧方案、历史基线、真实实证及兼容入口”的旧要求，以本次明确的清理决定为准；数据安全、不伪造证据及不操作无关服务的规则继续适用。

不推送、不重写 Git 历史、不创建第二次提交，不启动应用、数据库、部署或维护演练。不安装 Docker，不删除本机全局工具或共享缓存。清理后的改动保留在工作区，等待用户后续安排。

## 2. 提交与恢复边界

### 2.1 清理前必须提交

先只读核对 Git 分支、已有暂存区、修改和未跟踪文件、忽略规则、待删除目标及相关进程。核查本机 Git 姓名和邮箱，不覆盖现有配置。

将清理前本仓库相关的文档、计划、模板、应用源码和可入库证据纳入一次本地提交，包括尚未跟踪的内容。不要仅提交原来已经被 Git 跟踪的文件，也不要盲目暂存无法确认用途的新文件。

提交前检查敏感信息和暂存差异：不提交凭据、私钥、数据库、依赖缓存或构建缓存，不使用强制添加忽略文件的方式补齐快照。检查结果只报告路径和问题类别，不输出敏感值。

提交信息固定为：

```text
chore: snapshot repository before lifecycle cleanup
```

提交成功后记录完整提交号，并用该提交的文件清单核对待删除的可入库文件确已保存。未保存的非忽略内容不得直接删除；出现新改动时先重新核对来源。

**提交成功是硬性前提。** 提交失败、存在合并冲突、敏感文件无法安全排除或待删除源码未被快照覆盖时，停止清理。不得绕过安全检查、追加无关改动或为了继续而扩大删除范围。

### 2.2 哪些内容可恢复

| 内容 | 删除后的恢复方式 |
| --- | --- |
| 清理前提交包含的文件 | 可以从记录的提交恢复；本次不执行恢复、不回退整个工作区 |
| 被忽略的 `.runtime/`、本地数据库、依赖、构建及测试缓存 | 不进入提交，不另作备份，删除后不能通过 Git 恢复 |
| 仓库外全局工具、共享缓存和用户数据库 | 不在删除范围内，保持原样 |

Git 提交只保存可入库内容，不是完整本机运行环境备份。不可恢复的本地内容必须在清理清单和交付中明确列出。

## 3. 保留、改写与删除清单

### 3.1 保留并整理

| 区域 | 清理后的要求 |
| --- | --- |
| 根目录 | 保留 README、AGENTS、`.gitignore`、本清理计划和 v0.3 修订计划；保留 `.git` 及不属于旧方案的基础配置 |
| `model/` | 保留可复用总纲、约定、裁剪、制品规范、索引和跨阶段实践，删除旧方案专属表述与引用 |
| `model/stages/` | 保留并调整 verification、release、maintenance 三份通用指南；四项开发活动的详细指南仍待后续实施 |
| `templates/` | 保留现有 A01–A22 模板，清除对旧计划和本机实证环境的依赖 |
| `examples/focustask/` | 只保留教学 README、教学 AGENTS、`docs/context.md` 和 `docs/artifact-examples/` |
| `scripts/` | 保留文档校验和通用图解渲染工具，调整已删除输入的处理；增加必要的校验器测试 |
| `model/diagrams/` | 只保留可复用渲染配置和简短的生成说明，不保留旧六阶段图的源码及导出 |
| `execution/` | 保留重写后的 `status.md`，新增 `cleanup.md` 记录本轮清单、提交号、进度、检查和交付 |

README 和总纲只展示最新方向及当前实际可用的入口；注明四项活动的详细规范仍待完善。不能将旧六阶段作为当前主流程，也不能把清理后的文档架子称为 v0.3 已实施完成。

### 3.2 FocusTask 教学材料收敛

删除业务来源文档前，将教学示例需要的内容提取到 `examples/focustask/docs/context.md`：创建任务和任务列表的范围、术语、角色权限、字段约束、正常/异常流程、接口及界面状态、适用质量要求。保持已有教学规则一致，不扩展产品功能。

现有 A01–A22 填写示例继续保留，统一引用教学上下文和对应规范。将真实运行路径、历史发布号、完成结论及旧确认来源改为明确的教学设定或未执行说明；不把真实历史记录改名后冒充新版示例。

所有教学示例保留“教学填写示例”“不作通过结论”。不能要求读取已删除应用、数据库或证据；通用模板中的未来项目路径可以作为参数示例，但必须与当前仓库真实入口区分。

### 3.3 直接删除的内容

| 目标 | 删除范围与前置处理 |
| --- | --- |
| `AI_LIFECYCLE_IMPLEMENTATION_PLAN.md` | 旧实施计划全文；先移除保留文件对它的执行依赖 |
| `AI_PROJECT_DOCUMENTATION_GUIDE.md` 和根 `docs/` | 旧总指南及全部迁移兼容页；不再留占位入口 |
| `execution/` 下旧材料 | 删除旧 baseline、进度之外的旧决定/阻塞/迁移/验收/交付文件；保留并重写 status，不删除新建 cleanup 记录 |
| `examples/focustask/app/` | 旧应用及其依赖、构建、运行数据、测试缓存；先完成进程归属检查 |
| `examples/focustask/evidence/`、`evaluation.md` | 旧实证记录及评价 |
| FocusTask 其他业务和实证文档 | 教学所需内容提取后，删除 `docs/` 中教学上下文与填写示例以外的文件和空目录 |
| `scripts/audit-evidence.mjs` | 只服务于旧实证的审计脚本 |
| `model/stages/requirements.md`、`design.md`、`implementation.md` | 旧前三阶段指南；必要规则先并入保留的实践或制品规范 |
| `model/diagrams/` 的旧图 | 删除 lifecycle、collaboration、artifacts 三组 `.mmd`、`.svg`、`.png`，改写原图解说明，移除所有旧图片嵌入 |

以上是目标范围，不是允许使用宽泛通配符直接删除的命令。执行时生成逐路径清单，区分保留、改写、删除及忽略文件；未知新增文件不得仅因位于上级目录就自动删除。

删除前检查符号链接及真实路径。不得跟随链接删除仓库外内容，不删除仓库根目录或 `.git`，不使用全仓强制清理或硬重置代替逐项处理。

若待删运行目录仍被进程使用，确认 PID、命令及数据目录后，仅停止能够证明属于旧 FocusTask 任务的进程。无法确认归属则暂停相关删除，不影响用户其他数据库或服务。

## 4. 必须同步调整的内容

### 4.1 最新修订计划

精简重复说明，但保留四项活动、A01–A27 目标、提示词要求、七项任务、十项验收标准和十二项走查场景。同步改写与清理决定冲突的条款：

- 删除旧方案入口、仓库内外备份、保留旧实证及旧兼容路径的要求。
- 起始状态改为清理后的真实状态，教学上下文改为新入口；现有 A01–A22 路径继续稳定。
- V03-01 改为核对清理结果并建立新任务状态，引用清理前提交，不再复制旧文件形成 baseline。
- V03-05 不再迁移旧兼容页，改为整合新活动及制品索引、校验和导航。
- 将“历史保护有效”改为“清理边界与引用有效”；将历史实例场景改为教学材料独立可用检查。
- 将图解任务明确为根据新模型生成四组图，而不是依赖已删除旧源码。

本清理计划是 v0.3 的准备任务，不是另一套生命周期模型。清理结束后，根状态将下一任务指向修订计划，不自动开始执行。

### 4.2 指南、索引和工具

现有制品索引继续包含 A01–A22 及其规范、模板和教学示例，不在清理中新增 A23–A27。

文档检查器本轮只要求仍存在的 verification、release、maintenance 三份阶段指南；保留制品数量、唯一编号、必填章节、九类制品提示词、七类阶段提示词、教学边界及本地链接检查。完整“四活动＋三阶段”检查由后续 v0.3 任务完成，不能通过跳过所有阶段检查来清零错误。

为可重复验证增加 `--root <目录>` 可选参数，默认检查仓库根目录；使用 Node.js 自带测试能力和临时夹具建立 `scripts/check-docs.test.mjs`。缺失文件应产生明确诊断，不能只抛异常或静默跳过。无需新框架或应用依赖。

保留图解渲染脚本及现有参数。旧图删除后，脚本在调用外部渲染器前检查预期输入，缺少源码时明确提示“新版图解待建立”并非零退出；不下载工具、不生成替代旧图、不把缺输入报告成渲染成功。

检查所有保留正文中的旧阶段交接、已删除路径、旧计划启动指令和旧实证完成声明。清理计划和清理记录中的删除目标名称属于必要说明，可以保留为代码文本，不能作为活动链接或执行输入。

## 5. 执行顺序与恢复

| 任务 | 工作内容 | 完成条件 |
| --- | --- | --- |
| CLEAN-01 | 只读盘点、敏感信息检查、准备并创建清理前提交 | 提交成功；相关可入库文件已保存；记录提交号及忽略内容的恢复限制 |
| CLEAN-02 | 创建清理记录，提取教学上下文，整理保留规范和模板 | 示例不再依赖待删除来源；保留及删除清单明确 |
| CLEAN-03 | 更新规则与入口，按准确清单删除旧内容 | 删除范围符合清单，无无关服务、数据或文件被修改 |
| CLEAN-04 | 精简并校正最新修订计划，修复索引、校验器、渲染器和残留引用 | 工具与当前真实目录一致，新版待完成事项清楚 |
| CLEAN-05 | 执行检查、修复并复查，完成状态及交付记录 | 第 6 节全部必需标准通过，清理差异留在工作区 |

严格先提交再修改或删除现有内容。CLEAN-01 之前只允许只读检查及 Git 暂存/提交准备；不要先清理文件，再创建所谓清理前快照。

提交后在 `execution/cleanup.md` 记录提交号、逐路径清单、不可恢复内容、各任务状态、实际命令及结果。根 `execution/status.md` 保留当前任务和下一步，不重建旧任务历史。

中断恢复时，先核对记录的提交是否存在、工作区差异及已完成步骤，再继续未完成任务。不重复创建快照提交，不恢复已删除旧内容，不重做已经完成的教学提取。出现无法确认的新文件或进程归属时，只暂停相关部分并报告具体问题。

## 6. 验收与交付

| 验收项 | 通过标准 |
| --- | --- |
| 提交前置 | 有真实提交号；可入库删除对象被该提交覆盖，提交发生在清理之前 |
| 范围正确 | 清单中的旧内容已删除；只保留最新方案所需内容；`.git` 和仓库外资源未受影响 |
| 教学独立 | A01–A22 示例仅依赖保留规范、模板与教学上下文，不需要旧应用或实证 |
| 链接与结构 | 文档检查器退出码为 0，无断链、缺失制品、缺失提示词或教学边界 |
| 检查器有效 | 正常夹具通过；缺失文件、重复编号、断链、缺失提示词和教学边界均能被拒绝 |
| 状态准确 | 四项活动的详细指南、A23–A27、新图解和新增实证仍标为待实施；清理不等于模型完成 |
| 工具行为 | 渲染器缺少新版源码时给出预期诊断与非零退出，不误报成功 |
| 交付真实 | 清理记录包含提交号、删除范围、不可恢复内容、实际检查与下一步；未再次提交或推送 |

执行以下命令，记录真实结果：

```sh
node scripts/check-docs.mjs
node --test scripts/check-docs.test.mjs
node scripts/render-model-diagrams.mjs
git diff --check
git status --short
git diff --stat
```

检查器测试是本轮待建立入口。渲染命令预期因新图尚未建立而非零退出，该项只验证诊断行为，不能记为图解生成通过。测试使用临时夹具，不修改真实文档制造故障。

此外检查 Git diff 及未跟踪文件，进行语义走查：从 README 找到最新 plan、从任一制品找到模板和教学示例、从教学示例理解输入、不误启动旧应用或计划。对计划本身核对七任务、十验收、十二场景仍完整且含义已适配清理结果。

所有必需项通过才报告“仓库清理完成”。最终说明提交号、删除的主要目录、不可通过 Git 恢复的忽略数据、文档和脚本检查结果，以及下一步仍是执行 v0.3 修订计划。不要报告 v0.3 方法论已全部实施或验证。

## 7. 交给 goal 的执行文本

```text
请执行 AI_LIFECYCLE_V03_CLEANUP_PLAN.md，将仓库整理为只保留
最新生命周期方案、可复用规范与模板，以及 FocusTask 教学材料。

先读取根 AGENTS.md、本清理计划、v0.3 修订计划和执行状态。
按 CLEAN-01 至 CLEAN-05 推进，先检查并创建一次清理前本地提交，
确认相关可入库内容确已保存，才开始修改和删除。
提交失败或快照不完整则停止相关清理，不跳过此条件。

按准确清单直接删除旧方案、兼容入口、旧应用、实证和运行数据，
不移出仓库、不另建备份；不强制提交忽略文件或敏感信息。
忽略的数据库和运行数据无法通过 Git 恢复，记录实际删除范围。
只停止能够确认属于旧 FocusTask 的任务进程，不操作无关服务。

同步提取教学上下文，修复保留文件、计划、规则、导航和校验工具。
在 execution/cleanup.md 记录提交号、清单、进度、检查与恢复入口。
完成计划规定的文档、工具测试、语义及范围验收，修复后重验。

清理后不创建第二次提交，不推送，不启动完整 v0.3 建设或新增实证。
只在必需验收通过后报告仓库清理完成，并提供提交号、删除范围、
不可恢复内容、检查结果和下一任务；无法完成的事项如实记录。
```
